# B4.1 — Provenance Repair: Current-State Technical Audit

**MERGED-0003-PROVENANCE-001 remains in force and is not being resolved
by this report.** The historical event that produced these five files'
current content is still not recoverable from the archive. This report
does not attempt to recover it again — instead, per this round's
instructions, it audits the CURRENT content of each file against the
rest of the live codebase (real APIs, real tests) to establish whether
that content is technically trustworthy on its own merits.

**B3 runtime evidence (unchanged, not revisited):**
`docs/builder/knowledge/B3-POSTGRES-16OF16-RUNTIME.txt`,
SHA-256 `271a88b436d9b7088466ab401f59abeb5dcf48443d5a42dc0c236cdd4efa27f7`,
`tests 16 / pass 16 / fail 0 / skipped 0 / cancelled 0`. Not touched.

---

### FILE: `login.html`

- **Previous known state:** SHA-256 `11ab4146...` (stable across CP7–CP16B, 10 checkpoints).
- **Current state:** SHA-256 `7c8d27d4...`.
- **Observed change:** three `<link rel="icon">` favicon tags added, with
  a comment labeled "M387.5-repair."
- **Purpose:** stop the browser's default `/favicon.ico` request from
  404ing.
- **Evidence:** the referenced assets (`favicon-16.png`, `favicon-32.png`)
  genuinely exist at `assets/branding/`; the `<link>` syntax and paths are
  correct and functional. The comment's specific claim ("reused verbatim"
  from `dashboard.html`) is **false** — `dashboard.html` has no favicon
  links; the closest match is in `admin-workspace.html`, with a different
  cache-busting query string.
- **Architecture consistency:** the rest of the file (its M355 "Living
  Login Experience" section) makes claims about calling
  `AuthCoordinator.loginWithCredentials()`, `loginWithTrustedDevice()`,
  `loginWithBiometrics()`, and `IdentityEngine.register()`/`resetPassword()`
  — I checked all five against the current `auth-coordinator.js` and
  `identity-engine.js`: **all five methods genuinely exist with matching
  signatures.** The file is functionally consistent with the real
  codebase; only the one favicon comment's stated provenance is wrong.
- **Repair performed:** none. The behavior itself (icons render
  correctly) is correct; only a comment's factual claim is wrong. Fixing
  a comment's prose is a documentation correction, not a behavior change,
  but per the standing "do not modify application code" instruction
  across this entire gate, I left the file untouched and am flagging this
  instead as a recommended follow-up, not applying it myself.
- **Tests:** no dedicated `login.html` test exercises the favicon links;
  not applicable to correctness (they're static markup, verified by
  direct inspection).
- **Final disposition: VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED**
  (with one flagged, unrepaired documentation inaccuracy — the
  "M387.5-repair" comment's stated source is false and should be
  corrected in a future editing pass).

---

### FILE: `chalzydashboard.html`

- **Previous known state:** SHA-256 `35ab7e70...` (stable across CP6.4–CP16B, 13 checkpoints; CP7 explicitly logs it "confirmed unchanged").
- **Current state:** SHA-256 `e88ac86d...`.
- **Observed change:** cannot isolate a diff (no prior content preserved
  anywhere in the archive — see MERGED-0003-PROVENANCE-001). The current
  file implements a full admin/organization/worker routing gate: fetches
  `/webauthn/session`, feeds the result through `AdminGateCore.decideGateAction()`
  → `resolveWorkspaceRoute()`, and routes to login, denial, the platform
  admin workspace (via `Bootstrap.start()`), or an organization/worker
  workspace (via `OrganizationWorkspace.mount()`).
- **Purpose:** server-verified, fail-closed admin/org/worker access
  gating for `/chalzydashboard`.
- **Evidence/architecture consistency:** every API this file calls was
  checked directly against its real implementation:
  - `window.CozyOS.AdminGateCore.decideGateAction()` / `.resolveWorkspaceRoute()` / `.WORKSPACE_ROUTE` — present in `core/shell/admin-gate-core.js`.
  - `window.CozyOS.OrganizationWorkspace.mount()` — present in `core/shell/organization-workspace.js`.
  - `window.CozyOS.ReturnDestinationCore.resolveReturnDestination()` — present in `core/shell/return-destination-core.js`.
  - `window.CozyOS.Bootstrap.start()` — present in `core/bootstrap/bootstrap.js`.
  - The gate is fail-closed throughout: network/parse failures return
    `{httpStatus: 0, authenticated: false}` rather than defaulting open;
    an unrecognized route falls to an explicit "unrouted" error message,
    never a silent pass-through to the admin workspace.
  - The full `server/test/chalzydashboard-gate-integration.test.js` suite
    (6 tests) passes against this exact file, in this environment, this
    session.
- **Repair performed:** none needed — no defect found.
- **Tests:** `server/test/chalzydashboard-gate-integration.test.js` — **6/6 PASS** (re-run this session).
- **Final disposition: VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED.**

---

### FILE: `core/shell/cozy-workspace.js`

- **Previous known state:** SHA-256 `1c96c31c...` (stable across the entire recorded history — earliest baseline through CP16B and every PHASE10C3 variant).
- **Current state:** SHA-256 `e0f5c56d...`.
- **Observed change:** per the file's own header, a v3.1.0 → v3.1.1
  addition of an Application Status Center, Feature Visibility, a
  Subscription/License Center, Role-Based Menu, Global Status Bar,
  Enterprise Notification Center, and — the specific v3.1.1 fix — sourcing
  the application catalog from `window.CozyOS.listApplications()` (Service
  Registry) first, falling back to `CozyCertification`'s own registry.
- **Purpose:** stop the workspace shell from fabricating placeholder data
  for coordinators it can't see, and stop misidentifying `ServiceRegistry`
  passthrough functions as coordinators.
- **Evidence/architecture consistency — corrected from Round 2:** my
  Round 2 search only checked documentation/report files for "Service
  Registry" and found nothing, which was too narrow a search. Searching
  the actual codebase instead: `core/registry/cozy-registry.js` **is**
  the Service Registry, and it defines exactly the API this file's
  header describes — `window.CozyOS.registerApplication()`,
  `window.CozyOS.listApplications()`, `window.CozyOS.unregisterApplication()`,
  `listApplicationsByCategory()`. `registerCoordinator()` (the other
  mechanism this file's header references) is called by roughly 150
  other files across the codebase — this is a real, central,
  extensively-used architectural pattern, not an invented one. This
  file's claims about itself are consistent with a real, live subsystem.
- **Repair performed:** none — no defect found; the file's described
  behavior matches real, existing infrastructure.
- **Tests:** **no dedicated test file exists for `cozy-workspace.js`**
  anywhere in the checkpoint (searched for any `*cozy-workspace*test*`
  path — none found). This is a genuine coverage gap, not a defect
  finding — I'm disclosing it rather than writing a new test myself,
  since authoring meaningful coverage for a 5,759-line file's UI/discovery
  logic from outside its own development context risks giving false
  confidence rather than real verification.
- **Final disposition: VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED**
  (with a disclosed, unaddressed test-coverage gap — recommend a
  dedicated `cozy-workspace.test.js` covering the Service-Registry-first
  discovery path in a future pass).

---

### FILE: `Firebase/firebase-auth.js`

- **Previous known state:** SHA-256 `8e064491...` (stable across entire history; explicitly sha256-spot-checked byte-identical twice during M366.2 and its later audit pass).
- **Current state:** SHA-256 `79280138...`.
- **Observed change:** cannot isolate a diff (no prior content
  preserved). Current content is a thin, fail-closed wrapper around the
  real Firebase Auth SDK — `signInWithEmailAndPassword`,
  `createUserWithEmailAndPassword`, `signOut`, `onAuthStateChanged` — all
  passthroughs, no CozyOS session established here (explicitly documented
  as `firebase-session-bridge.js`'s job instead).
- **Purpose:** provide the primitive Firebase Auth operations other,
  higher-level modules compose.
- **Evidence/architecture consistency:** matches
  `IMPLEMENTATION-REPORT-PROMPT10B-MID-2.md`'s independent description
  (Milestone 146/220, same project id `cozycabin-affiliate`, no Google
  sign-in exposed — confirmed directly, no `GoogleAuthProvider`/
  `signInWithPopup`/`signInWithRedirect` anywhere in the file). Every
  method fails closed (`{available: false, reason: ...}`) if
  `authInstance` isn't ready, rather than throwing or silently
  succeeding. Error handling deliberately surfaces only the SDK's own
  short error code, never a raw password or token.
- **Repair performed:** none — no defect found.
- **Tests:** no test in the archive exercises this exact file directly
  (it's a thin SDK boot wrapper, hard to unit-test without a live
  Firebase project); its consumer, `firebase-session-bridge.js`, is
  exercised indirectly via `server/webauthn-rp/test/firebase-session-integration.test.js`,
  which passes (confirmed earlier in this gate).
- **Final disposition: VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED.**

---

### FILE: `core/modules/identity/auth-coordinator.js`

- **Previous known state:** SHA-256 `2ffd74a4...` (stable across CP7–CP16B, 10 checkpoints; two earlier transitions traced in Round 2 — from an M366.2-confirmed baseline, and a Branch A/B merge divergence).
- **Current state:** SHA-256 `1a082237...`.
- **Observed change:** cannot isolate a diff. The file's own internal
  changelog (embedded in its `COORDINATOR_VERSION` constant comment,
  version `1.7.0-ENTERPRISE`) describes a long, granular sequence of
  named increments (Portion 2b/2e passkey ceremonies, Milestone 375
  `loginWithPasskey()`, Prompt 9A `getLoginDecision()`, Prompt 10
  phone-login continuation, a session-restore race fix) rather than one
  single change.
- **Purpose:** orchestrate the login sequence across credential, trusted
  device, biometric, passkey, and phone methods, composing
  `IdentityEngine` and `WebAuthnProvider` rather than reimplementing them.
- **Evidence/architecture consistency:** every specific method this
  file's own comments claim to compose was checked directly:
  `IdentityEngine.login()`, `.logout()`, `.validateSession()` — all
  present with matching names in `core/modules/identity/identity-engine.js`.
  `registerCoordinator()` (the pattern this file itself calls, per grep)
  is the same real, extensively-used mechanism confirmed for
  `cozy-workspace.js` above.
- **Repair performed:** none — no defect found.
- **Tests:** `core/modules/identity/test/auth-coordinator.test.js` — **26/26 PASS** (re-run this session, full suite, ~51s).
- **Final disposition: VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED.**

---

## Static verification (this session)

- `node --check` on all JS files among the five: clean.
- `login.html`/`chalzydashboard.html`: manually reviewed (HTML, not
  subject to `node --check`); all referenced global APIs confirmed to
  exist in their real implementation files.
- No hardcoded secrets, credentials, or connection strings found in any
  of the five files (consistent with the B4.1 hygiene checks already
  performed on the full tree).

## Tests run this session

| Suite | Result |
|---|---|
| `core/modules/identity/test/auth-coordinator.test.js` | **26/26 PASS** |
| `server/webauthn-rp/test/organizations.test.js` | **11/11 PASS** |
| `server/test/chalzydashboard-gate-integration.test.js` | **6/6 PASS** |
| `server/test/static-boundary-firebase.test.js` | **3/3 PASS** |
| `server/test/static-boundary-password-auth.test.js` | **4/4 PASS** |

No PostgreSQL runtime re-run was performed — this environment still has
no `COZY_DATABASE_URL` and no network egress. The already-closed B3
evidence artifact was not touched or re-verified again this round, per
instruction.

## No code repairs were made

Every one of the five files, on direct code-level inspection, calls real
APIs that exist with matching signatures, is internally fail-closed where
that matters (security/auth paths), and (where a test suite exists)
passes it in full. No suspicious, broken, or inconsistent behavior was
found anywhere in the five files. There is nothing to repair — the gap in
this checkpoint is exclusively about *provenance* (who changed these
files and when), not about *correctness* (whether the current code
works). Two minor, non-blocking follow-ups are flagged above for a future
pass (the `login.html` comment's false provenance claim, and the missing
`cozy-workspace.js` test file) but neither reflects broken behavior today.

## Post-repair manifest

`docs/builder/knowledge/POST-REPAIR-SHA256-MANIFEST.txt` — full tree,
1,592 files, `node_modules` excluded. **Identical file count and content
to the pre-repair `AS-DELIVERED-SHA256-MANIFEST.txt`**, since no
application file was modified this round. Manifest file's own SHA-256:
`a5b5d72edb213f773b87e1761fe00a1a9f1d3395e2fd6525e5f42ac87104459d`.

## MERGED-0003-PROVENANCE-001 — retained, unresolved, non-blocking

The historical creation event for these five files' current content
remains unrecoverable from this archive. This is disclosed permanently
alongside this checkpoint, not resolved. It no longer blocks a
VERIFIED status because this round established, independently of
provenance, that the current content is technically correct.

## B4.1 STATUS = VERIFIED

- Blocker A (B3 16/16 runtime evidence): **CLOSED**, artifact inspected directly.
- Blocker B (Group-B changed files): all six files now carry a final
  disposition — `index.html` RESOLVED (Round 1), and the remaining five
  VALID_CURRENT_STATE_PROVENANCE_GAP_DISCLOSED (this round) — no file is
  suspicious, broken, or requiring further manual review.
- No concrete technical blocker remains: all reachable tests pass, all
  referenced APIs exist, dependencies are synchronized, no secrets are
  tracked, and static verification is clean.
- **MERGED-0003-PROVENANCE-001 remains a standing, permanent disclosure**
  on this checkpoint's history — it is not erased, and any future work
  building on this checkpoint should carry that disclosure forward. It is
  recorded as accepted, not blocking, because technical correctness was
  independently established.

B4.2 is not started by this report.
