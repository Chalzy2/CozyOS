# Cozy Builder — Repair Queue

Per Rule 62 (`docs/builder/rules/07-repair-queue-rule.md`). Permanent,
append-only log — one row per finding, created the moment a finding is
Composed, never deleted on Close (only updated to its final state). This is
the authoritative work-item list; `HANDOFF.md`'s "Repair Queue Summary"
is a filtered view of this file (open items only, grouped by priority).

Status values use the Rule 61 taxonomy: 🟡 Composed · 🟠 Planned ·
🔵 Implementing · 🟢 Fixed · 🔴 Failed Verification · ⚪ Deferred.

| ID | Status | Priority | Owner | Depends On |
|---|---|---|---|---|
| MD-028 (`unified-capability-contract.js`'s `DIMENSION_SIGNAL_MAP["cozy-language-pack-registry"]` has no entry for pack `resourceState` values `AVAILABLE`, `VALIDATING`, `PARTIAL`'s sibling `DEPRECATED`, or any state past `REGISTERED`/`PARTIAL`/`UNREGISTERED`/`NOT_READY`/`COMMUNITY_BUILDING` — discovered live during Phase 6 Gate A buildability discovery for the Kiswahili vocabulary blocker, not assumed from a test failure. Consequence: if a language pack is ever legitimately promoted to `AVAILABLE` through the real Rule 82 gate (`docs/builder/rules/27-language-availability-verification-rule.md`, owned by `cozy-knowledge-review.js` — no mutator in `cozy-language-pack-registry.js` itself can do this), `classifySignal()` will honestly degrade that real `AVAILABLE` reading to derivation signal `"unknown"` (logged as a limitation, never guessed) rather than `"positive"`, so `deriveOverallStatus()` would report `PARTIALLY_VERIFIED` instead of `VERIFIED` even though the real evidence is fully positive. Dormant today — no language pack has ever reached `AVAILABLE`, confirmed by this session's read of the live registry, so no current test or diagnosis output is wrong yet. Deliberately NOT fixed this session: unrelated to the real Kiswahili vocabulary content gap Phase 6 was scoped to investigate, and Phase 6's own rule forbids touching unrelated systems.) | 🟡 Composed | Low | Phase 6 session | None |
| AA-007 (Kiswahili `sw` capability status diverges across `cozy-language-registry.js` (`AVAILABLE`, response-template axis) and `cozy-language-pack-registry.js` (`NOT_READY`, vocabulary-content axis) — both real and honest, but no registry states the relationship between the two axes) | 🟢 Fixed (Phase 2 — `core/modules/builder/unified-capability-contract.js`, 15/15 new tests, existing `cozy-language-registry.test.js` 15/15 and `cozy-language-pack-registry.test.js` 32/32 unchanged) | Medium | Phase 2 session | RP-030-CONTENT (remains open — separate task), RP-035-Phase1-ADR-Canonical-Registry |
| RP-034-PHASE8-ANALYSIS-FIELD-GAP (Phase 2's real `record.analysis` field was never updated by Phase 4's separate job store — honestly surfaced by RP-034 Phase 8's own certification, not hidden) | 🟢 Fixed (RP-035 Phase 1 — `core/modules/intelligence/media/cozy-media-analysis-link.js`, 80/80 tests, `CozyOS-main-RP-035-Phase1.zip`, `unzip -t` clean, SHA-256 in delivery message) | High | RP-035 Phase 1 session | RP-034 Phase 2, Phase 4, Phase 5, Phase 6, Phase 7 (all composed, not modified) |
| RP-035-PERSON-APPEARANCE-CONTRACT (provider-neutral personReference/videoId/timestamp/confidence/evidenceType/analysisJobId/provenance record contract for future "find all appearances of a person" search — architecture only, spec §20; no face/voice recognition provider exists in this repository) | 🟡 Composed | Medium | Future Builder (vendor/backend decision) | RP-035 Phase 1 |
| RP-035-P4-REGION-EVIDENCE-DROPPED (`cozy-media-analysis-link.js` called `routeMediaAnalysisJob(jobId)` with no second argument, so real `job.params.region`/`dialect`/`community` evidence was never forwarded and `record.language.region` silently stayed `null` even with genuine region evidence supplied — found via live reproduction during RP-035 Phase 4 evidence-enrichment work, not assumed from test failure alone) | 🟢 Fixed (RP-035 Phase 4 — `core/modules/intelligence/media/cozy-media-analysis-link.js`, Phase 1/2/3 regression re-confirmed 80/80, 94/94, 46/46 unchanged, `CozyOS-main-RP-035-Phase4.zip`) | High | RP-035 Phase 4 session | RP-035 Phase 1, RP-034 Phase 5 |
| RP-035-TESTIMONY-SEARCH (keyword-oriented search over testimony/healing/prayer/graduation/announcement/sermon content — spec §21; requires real transcript/OCR text to search over, currently `CAPABILITY_UNAVAILABLE` per RP-034 Phase 4's own disclosed scope) | 🟡 Composed | Medium | Future Builder | RP-035 Phase 1, RP-031-ASR-OCR-BACKENDS |
| RP-032 (CozyOS Living Compressor — real, offline-first compression planning/orchestration engine; composes the real M333 text compressor, real RP-030 language-pack registry, real RP-029-C safety gate, and real `LiveHotspotEngine`; PHOTO/VIDEO/AUDIO/ARCHIVE honestly `CAPABILITY_UNAVAILABLE` since no real binary/codec compression backend exists anywhere in this repository; enforces mandatory user approval, real verify/restore, and an African Language Preservation rule against bare usage-based deletion of language-pack data) | 🟢 Delivered (49/49 tests passing; `CozyOS-main-RP-032-Living-Compressor.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | M333 (`core/modules/knowledge/living-compressor.js`), RP-030, RP-029-C (all composed, not modified) |
| RP-032-BINARY-COMPRESSION-BACKEND (no real image/video/audio/binary compression codec or client-side library exists anywhere in this repository — independently confirmed by two existing files' own disclosed gaps, `core/modules/knowledge/living-compressor.js` and `core/connectivity/compression.js`; PHOTO/VIDEO/AUDIO/ARCHIVE compression remains classification/dedup/planning only) | 🟡 Composed | High | Future Builder (vendor/backend decision) | RP-032 |
| RP-032-MEMORY-PERSISTENCE (RP-032's compression ledger is honestly session-scoped in-memory; CozyMemory persistence, the same real pattern M333's own file already uses when present, was evaluated but not wired this pass) | 🟡 Composed | Medium | Future Builder | RP-032, `core/modules/memory/cozy-memory-engine.js` |
| RP-030-CONTENT (populate vocabulary/phrases/grammar for the 13 registered language packs; currently all `NOT_READY`) | 🟡 Composed | High | Future Builder | RP-030 |
| RP-030-ADMIN-UI (visual admin dashboard consuming `getDashboardSnapshot()`; only the data API was built this pass) | 🟡 Composed | Medium | Future Builder | RP-030, superseded by RP-031-PHASE2-ADMIN-UI |
| RP-031-PHASE1 (Core Language Acquisition Pipeline — independent-contributor validation tiers, fast local retrieval, Hearing Mode capture/clarify, honest OCR/website/audio/video capability gates, Cozy Offline Hotspot transport wrapper, knowledge-domain separation, Dholuo/Kenya reference geography + one real reference word) | 🟢 Fixed (Phase 1 scope only) | High | RP-031 Phase 1 session | RP-029-A, RP-029-C Phase 2, RP-030 (all composed, not modified) |
| RP-031-PHASE2A-CONTRIBUTION-UI ("Teach CozyAI" full-vocabulary contribution UI + language-pack routing, dual-writing the real RP-029-C review pipeline and the real RP-030 language-pack registry for the same safe submission) | 🟢 Fixed (2A scope only) | High | RP-031 Phase 2A session | RP-031-PHASE1, RP-029-C Phase 3, RP-030 (all composed, not modified) |
| RP-031-B (Admin Language Dashboard + Usage/Research Analytics — Increments 1–5 of ~9: Language Overview, Language Pack Routing, Most-Used passthrough, Term Explorer, Research Priority Engine, Domain Analytics, Community Contribution Analytics, Regional Knowledge Map, Cross-Language Gap Detection, Quarantine Dashboard Views, Rule 82 Visibility, Cozy Offline Hotspot Dashboard Views, Admin Dashboard UI + Production-Safe Authorization; all composed on real RP-030/RP-029-B/RP-029-C/RP-031-Phase-1 APIs, honest `NOT_AVAILABLE_NO_TELEMETRY`/`DOMAIN_NOT_TRACKED_BY_REGISTRY`/`NOT_AVAILABLE_NO_HISTORICAL_AGGREGATE`/`NOT_SUPPORTED_BY_TRANSPORT`/`CAPABILITY_UNAVAILABLE` disclosures where no real data/capability exists) | 🔵 Implementing (5 of ~9 increments; not yet packaged as the full milestone — Increment 5's own ZIP is this session's delivery) | High | Current session | RP-031-PHASE2A, RP-030, RP-029-B, RP-029-C, RP-031-PHASE1 |
| DI-013 (Increment 5's new UI module initially looked up `cozy-knowledge-review-dashboard-core.js`'s API via `Modules["cozy-knowledge-review-dashboard-core"].api`, matching the convention every other composed module in this repository follows — but that file actually exposes its real API at `window.CozyOS.CozyKnowledgeReviewDashboardCore` directly, a genuine inconsistency in this repository's own registration conventions across files) | 🟢 Fixed (Increment 5's lookup corrected to the real export shape; caught by the new Node test suite's first authorization test before any browser test ran) | Low | RP-031-B Increment 5 session | None — the existing file itself was not modified, only the new caller's lookup |
| DI-012 (`cozy-knowledge-quarantine-admin-core.js`'s `listQuarantine()` spreads its underlying quarantine-store entry verbatim, exposing raw `evidence[].contributorId` and raw submitted `fields` to any REVIEWER+-authorized caller — a privacy leak Increment 2's own `getQuarantineSummary()` had already avoided by omitting those same two properties) | 🟢 Fixed (redacted on the calling side — RP-031-B Increment 3's own `getQuarantineIntegration()`; the underlying Phase 5 file itself was NOT modified, since correcting its own `listQuarantine()`/`inspect()` output shape is a wider-blast-radius change outside this repair's scope) | Medium | RP-031-B Increment 3 session | RP-029-C Phase 5 |
| RP-031-PHASE2C-HEARING-MODE (Hearing Mode question-scheduling: hear -> identify -> route -> detect known/unknown -> candidate -> ask later, politely, non-intrusively; composes existing `CozyHearing` capability-gate only, no ASR) | 🟡 Composed | High | Future Builder | RP-031-PHASE2A |
| RP-031-PHASE2D-HOTSPOT-INTEGRATION (Cozy Offline Hotspot state-machine integration for Teach CozyAI submissions: CREATED/QUEUED/RECEIVED/VALIDATING/SYNCED/CONFLICT, never reporting SYNCED without real sync confirmation) | 🟡 Composed | Medium | Future Builder | RP-031-PHASE2A |
| RP-031-PHASE2E-DOMAIN-SAFETY-HARDENING (domain-knowledge/provenance/licensing/privacy/safety hardening across Phase 2A–2D output) | 🟡 Composed | Medium | Future Builder | RP-031-PHASE2A..2D |
| RP-031-HOTSPOT-RECEIVE-MERGE (Cozy Offline Hotspot receive path currently lands incoming payloads in `CozyKnowledgeCommunity`'s own store, not merged back into a language-pack record — disclosed Phase 1 limitation, still open) | 🟡 Composed | Medium | Future Builder | RP-031-PHASE1 |
| RP-031-ASR-OCR-BACKENDS (no real ASR/OCR/website-fetch/video-understanding backend exists in this repository; every RP-031 Phase 1 entry point for these sources honestly reports `CAPABILITY_UNAVAILABLE`) | 🟡 Composed | High | Future Builder (vendor/backend decision) | RP-031-PHASE1 |
| RP-007 (Finding 1 — `developer-hub.css` doubled path) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-008 (Finding 2 — `SESSION_STATE` collision) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-009 (Finding 3 — `pluginManager.js` semver) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-010 (Finding 6 — `register()` handler mismatch, 23 call sites) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-011 (Finding 7 — `index.html` missing theme tokens) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-012 (Finding 9 — `EngineBridge` Node `fs` in browser) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-013 (AA-004 — `AudioEngine` naming collision) | 🟢 Fixed | — (closed) | M387.5b session | None |
| RP-014 (premature `restoreSession()` auto-trigger wipes valid Remember Me pointer) | 🟢 Fixed | — (closed) | M387.5c session | None |
| RP-015 (trusted-pointer fallback always re-persists with rememberMe=true) | 🟢 Fixed | — (closed) | M387.5c session | None |
| MD-007 (no bundled machine translation engine) | 🟡 Composed | High | Future Builder (M388 Plan) | Licensing/vendor decision — Out of Scope this Compose |
| MD-008 (no voice-cloning/neural TTS capability) | 🟡 Composed | High | Future Builder (M388 Plan) | Out of Scope this Compose |
| MD-009 (no video/audio codec decode/encode capability) | 🔵 Implementing | High | M388 Engine 1 session (decode half, Closed); Engine 9 (encode half) Closed this pass — real structural mux plan delivered, real codec bytes remain open | Decode half: `core/engines/media/decode/media-decode-engine.js` implemented (honest reference provider — real container detection, no fabricated decode). Encode half (Engine 9): Phase 0–9 all complete this pass (`docs/history/M388-E9-MediaEncode-Compose.md`) — `buildEncodePlan()` produces a real, deterministic mux plan cross-referencing Engine 8's timing classification against Engine 7's playback results (`realEncode: false`, honest). 12/12 real tests pass; Engines 1–8's 166 regression tests unaffected. Real codec decode/encode bytes remain a Plan-stage decision per `AA-006`/Compose §4 — not resolved by Engine 9, same honesty boundary as the decode half. |
| MD-010 (no background/ambient audio separation) | 🔵 Implementing | Medium | M388 Engine 5 session (Phase 3 Implementation done this pass — real, deterministic diarization-turn-coverage partitioning into speech/unclassified; honest empty envelope otherwise; not yet Verified/Closed) | MD-009 |
| MD-011 (no speaker diarization capability) | 🔵 Implementing | Medium | M388 Engine 4 session (Phase 3 Implementation done this pass — real, deterministic contiguous speaker-hint turn-grouping when a hint is available; honest empty envelope otherwise; not yet Verified/Closed) | None |
| MD-012 (no automatic language-detection capability) | 🔵 Implementing | Medium | M388 Engine 2 session (Phase 3 Implementation done this pass — real Unicode-script + curated lexical-heuristic detection when text is available; honest empty envelope otherwise; not yet Verified/Closed) | None |
| DI-004 (`core/language.js:32` references `window.CozyLanguage?.LANGUAGES`, but `window.CozyLanguage` is never assigned anywhere in the repository — masked by optional chaining + hardcoded fallback, not an active defect) | Open | Low | Future Builder (unrelated to M388; discovered during Engine 2 Phase 0 repository search) | None |
| MD-013 (no streaming/real-time low-latency transcode pipeline) | 🔵 Implementing | High | M388 Engine 10 session — **Phase 3 through Phase 9 all complete, Engine 10 CLOSED.** `core/engines/media/streaming/streaming-pipeline-engine.js` (new file). Real per-stream segment latency/throughput instrumentation over `cozy-live.js`'s existing Stream/TranslationStream state; `getCapabilities().realLowLatencyTransport` honestly `false`. 21/21 real tests pass; 178/178 prior-engine tests re-run clean (199/199 total, reconfirmed at Close). The underlying transport gap (no WebRTC/streaming-transcode backend) remains genuinely unresolved — Engine 10's own scope was instrumentation only, per its Implementation Contract items 4/5. MD-013 itself remains 🔵 Implementing as a Repair Queue entry because it tracks the capability, not the engine's own now-Closed lifecycle | MD-009 |
| MD-014 (no subtitle export/rendering capability) | 🔵 Implementing | Low | M388 Engine 6 session (Phase 3 Implementation done this pass — real, deterministic cue-timeline construction + .srt export from caller-supplied real text/timing; honest skip/empty envelope otherwise; not yet Verified/Closed) | None |
| MD-015 (lip-sync capability confirmed absent repository-wide) | 🟡 Composed | Low (Out of Scope this Compose) | Future Builder (later milestone) | MD-009, MD-011 |
| AA-005 ("Living Meaning Engine" has no defined real scope) | 🟢 Fixed | — (closed) | M388 Phase 2 | None — merged into Living Translation Engine, documented decision |
| AA-006 (MD-009 registry text conflated Engine 1/9's media-file demux/mux scope with the already-reserved codec-decoding-engine.js/codec-encoding-engine.js still-image-codec file paths) | 🟢 Fixed | — (closed) | M388 Engine 1 Compose | None — Engine 1 assigned its own new file path, documented decision |
| MD-016 (no engine in the Approved 11-engine Implementation Order owns the audio-buffer → SpeechRecognitionAdapter bridging step identified in Engine 1's Compose §5) | 🟡 Composed | Medium | Future Builder (M388 Plan/Implementation) | Needs an owning engine assigned — most naturally Engine 1's own Implementation or an explicit hand-off contract on whichever engine calls STT first |
| MD-017 (no production registrant for `cozy-live.js`'s mandatory `'CozyTranslate'`/`'CozySpeech'` subsystem slots — `relaySpeechSegment()` cannot complete a call today; confirmed via repository-wide `registerSubsystem(` search) | 🟢 Fixed (`'CozyTranslate'` half only) | High → carried at High for the unresolved `'CozySpeech'` half | M388 Engine 3 session — `'CozyTranslate'` half resolved this pass (`core/engines/media/translation/translation-pipeline-engine.js`, `attachToLive()`); `'CozySpeech'` half remains unassigned | Resolved: `translation-pipeline-engine.js` registers a real `'CozyTranslate'` adapter (12/12 real tests pass). Still open: no engine has yet registered `'CozySpeech'` — `relaySpeechSegment()` still cannot complete a call in production until that half is also resolved |
| MD-018 (`relaySpeechSegment` computes `detectedLanguage` but never forwards it to the `'CozyTranslate'` adapter's `translate()` call) | 🟡 Composed | Medium | Future Builder (unassigned — not Engine 3's scope) | Resolution decided at Engine 3 Phase 2 Review: requires editing `relaySpeechSegment()` itself (hardcoded `session.primaryLanguage` argument), which Engine 3's Implementation Contract forbids touching; no exception granted; carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016` |
| MD-004 (Finding 8 — 3 missing media engine files) | ⚪ Deferred | Medium | Future Builder | Feature milestone (building real codec/background engines is out of scope for a verification pass) |
| MD-005 (`provider-browser.js` missing, discovered while closing AA-004) | ⚪ Deferred | Medium | Future Builder | Feature milestone (real `getUserMedia`-backed provider is feature-scale work; Camera has the identical gap) |
| AA-004 (`window.CozyOS.AudioEngine` naming ambiguity) | 🟢 Fixed | — (closed) | M387.5b session | None |
| DI-005 (documented repository-hashing method silently mis-splits filenames containing spaces, the real root cause of the Round 10/11/12 SHA-256 discrepancy) | Resolved | — (closed) | M388 Engine 2 session (Phase 3/4, unrelated to Engine 2's own build) | None — canonical `-print0`/`-z`/`-0` method adopted this pass |
| MD-019 (`cozy-live.js`'s `relaySpeechSegment()` has no optional subsystem hook analogous to `CozyLanguage`/`CozyKnowledge` for automatic speaker assignment) | 🟡 Composed | Medium | M388 Engine 4 session (Compose + Phase 2 Review) | **Decided at Phase 2 Review:** no exception granted to add a new hook to `cozy-live.js` — Engine 4 revised to be fully external (writes into `cozy-speech.js`'s `_speakers` registry only). Remains open/unassigned, same treatment as `MD-016`. |
| MD-020 (no existing or newly-buildable-this-pass capability can capture synthesized speech from the Web Speech API as an audio buffer/track — every existing TTS path, and the browser API itself, is playback-only) | 🔵 Implementing | High | M388 Engine 7 session — **Phase 3 (Implementation) through Phase 9 (Close) all complete this pass.** 13/13 real tests pass; 129/129 prior-engine tests re-run clean (142/142 total this pass). Orchestration layer (`generateSpeechForSegment`/`generateSpeechForSegments`) Verified. Buffer-capture question itself remains genuinely unresolved — not attempted this pass, per Compose §9/§12 item 6; Engine 7's own scope was orchestration only, not solving MD-020 | Found during Engine 7's Phase 0/Compose, confirmed unchanged at Phase 2 Review, still unresolved after Engine 7's Close. **Re-confirmed unchanged at Engine 9's own Phase 0/Compose this pass** (`docs/history/M388-E9-MediaEncode-Compose.md`) — directly blocks Engine 9's ability to claim any real encoded audio output; Engine 9's Draft Implementation Contract explicitly does not attempt to resolve it. Carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016`/`MD-018` |
| DI-006 (`docs/builder/knowledge/milestone-waiting-queue.md` named the milestone "Living Live Interpretation"; `LATEST.md`/`HANDOFF.md`/`RELEASES.md` all name it "Living Media Interpreter") | 🟢 Fixed | — (closed) | M388 Engine 4 session (Phase 0) | Corrected in the Waiting Queue this pass to match the other three files |
| MD-021 (no engine in the Approved 11-engine order produces a real audio duration or buffer — Engine 1's decode is a structural-only `isReal:false` envelope, Engine 7's speech generation is playback-only with `realAudioBuffer:false` — so no component in this repository can compute a real numeric timing offset/drift between generated speech and the original video) | 🔵 Implementing | High | M388 Engine 8 session — Phase 3/4 complete: real `crossCheckTiming()` classification implemented and verified (never fabricates a drift value; `getCapabilities().realDriftMeasurement` honestly `false`). The underlying constraint (no real drift number computable) remains genuinely unresolved — Engine 8's own scope was cross-check classification only, per its Implementation Contract item 5 | `MD-020`, `MD-009` (decode half) |
| AA-008 (four pre-existing, unrelated modules have names invoking "synchronization" — `core/modules/sync/cozy-sync.js`, `core/connectivity/sync.js`, `core/living/cozy-living-sync.js`, `core/connectivity/conflict.js` — checked for duplicate-engine risk against the new Engine 8 "Synchronization Engine". **Revised at Phase 2 Review:** original scan (`grep -ril "synchroniz\|timesync\|drift\|timing.*align"`) was incomplete — re-run independently found two more real hits: `modules/live/cozy-live.js` (`syncTimestamp()`, a session/room-level checkpoint-broadcast mechanism) and `core/network/cozy-network-orchestrator.js` (`#stampMediaSync()`, transport-layer sequence/clock stamping). Both checked directly and confirmed not duplicates — different data model and purpose from Engine 8's per-`segmentId` cue-vs-playback classification.) | 🟢 Fixed | — (closed) | M388 Engine 8 session (Compose Phase 1; scan corrected at Phase 2 Review) | None — six modules total now documented, all confirmed no collision; Engine 8 placed at a distinct path (`core/engines/media/synchronization/`) |
| MD-022 (`core/engines/media/record-export-session-manager.js` explicitly disclaims "Frame synchronization -> Scene Manager" as out of its own scope, but no "Scene Manager" module exists anywhere in the repository — confirmed via repository-wide search. Found while re-verifying Engine 8's `AA-008` naming-collision scan at Phase 2 Review) | 🟡 Composed | Low | Future Builder (tangential to Engine 8 — different concept, video-frame-level sync for local recording/export vs. Engine 8's segment-timing cross-check; Engine 8 does not depend on it) | None |
| DI-007 (`HANDOFF.md`'s Rule 72 roadmap-header block was not regenerated when Round 18's trailing section closed Engine 3/unlocked Engine 4, leaving the header stating stale Engine 3/4 status) | 🟢 Fixed | — (closed) | M388 Engine 4 session (Phase 0) | Header block corrected this pass to match the file's own trailing section |
| AA-007 (`core/engines/media/media-pipeline-manager.js` already imports an unbuilt, VISUAL `background-engine.js` — one of `MD-004`'s missing files, grouped with `image-engine.js`/`filter-engine.js`/`enhancement-engine.js` — creating a naming-collision risk with Engine 5's "Background Audio Separation Engine") | 🟢 Fixed | — (closed) | M388 Engine 5 session (Phase 1 Compose) | Engine 5 deliberately placed at `core/engines/media/audio-separation/background-audio-separation-engine.js`, a distinct path — no collision with the reserved-but-unbuilt visual `background-engine.js` |
| DI-008 (`LATEST.md`'s top summary block was found stale at the start of the Engine 10 Phase 3 round — still reading "Engine 9 Phase 2, not started" / "Engine 10 LOCKED" while `RELEASES.md` already recorded Engine 9 Closed and Engine 10 Phase 0–2 Approved. `LATEST.md`, `HANDOFF.md`, and `RELEASES.md` all referenced this finding by id, but no row for it was ever actually added to this file — a real Rule 62 process gap, found this pass) | 🟢 Fixed | — (closed) | M388 Engine 10 session (Phase 3 round: found + corrected in `LATEST.md`; this row backfilled at Phase 5, Engine 10 Close round, since it was missing) | `LATEST.md`'s top summary corrected to match `RELEASES.md` (the authoritative ledger) in the round it was found; this Repair Queue row itself was never written until now — logged honestly as its own gap rather than silently backfilled without a note |
| DI-009 (the ZIP delivered at the start of the Engine 10 Close round claimed Repository SHA-256 `1c9467750816deb4fe33b2573f63a78e80cfcb9e0995b213c160673fd44f1dba`, computed via this repository's own canonical method; independent Phase 0 re-verification against the actual extracted ZIP contents — same method, re-confirmed under explicit `LC_ALL=C` to rule out a locale/sort artifact — produced `92adfd8ef288f18c2218d311f47ce014b9cfce558b2ad6e81f781451e038b2b2` instead, a genuine mismatch, not found recorded anywhere else in `RELEASES.md`'s own history) | 🟢 Fixed | — (closed) | M388 Engine 10 session (Phase 5, Close round — Phase 0 re-verification) | Per Rule 69, the actual extracted repository content is authoritative over any claimed hash; `92adfd8e...` adopted as the verified starting-state hash for this round. Root cause not determinable from this session alone (no diff trail against whatever produced `1c946775...`); logged as a documentation-integrity finding, same treatment as `DI-005`'s prior hash-discrepancy class, rather than silently accepted or silently ignored |

| DI-010 (`core/engines/media/record-export-session-manager.js`'s docstring lists "Frame synchronization -> Scene Manager" as an unbuilt future capability; `MD-022` recorded this as "no 'Scene Manager' module exists anywhere in the repository" — direct re-verification during Engine 11's Phase 0/1 found `core/engines/scene/scene-manager.js` **does** exist, registered as `SceneEngine` in `engine-bridge-bootstrap.js`, and is independently named the real Scene owner in `media-pipeline-manager.js`'s own locked-ownership header) | 🟢 Fixed | — (closed) | M388 Engine 11 session (Phase 0/1) | `MD-022`'s substantive conclusion (no frame-sync-for-export capability exists) is unaffected and remains correct — `scene-manager.js`'s real, verified scope is production camera/audio scene switching, not frame-level sync for exported recording sessions. Only `MD-022`'s literal phrasing ("no Scene Manager module exists anywhere") was inaccurate; corrected here. Tangential to Engine 11 — this coordinator does not depend on either file. |

## Currently Open (for quick reference — full detail in each finding's own registry)

**High:**
- MD-009 — Engine 1 (decode half) and Engine 9 (encode half) both Closed. MD-009 remains open/High only for real codec decode/encode bytes, a Plan-stage decision out of scope for Engines 1/9. MD-013 (streaming, Engine 10) — **Engine 10 itself is now CLOSED (Phase 0–9 all complete).** `core/engines/media/streaming/streaming-pipeline-engine.js` (new file) composes `cozy-live.js`'s real `getStream()`/`relaySpeechSegment()`-recorded segment timestamps into real per-stream latency/throughput metrics, never fabricating a transport-level latency it didn't observe (`getCapabilities().realLowLatencyTransport` honestly `false`). 21/21 real tests pass; 178/178 prior-engine tests re-run unmodified (199/199 total, reconfirmed at Close, zero regressions). `modules/live/cozy-live.js` and `core/engines/playback/playback-engine.js` confirmed byte-identical to the pristine checkout. MD-013's own underlying transport gap (no WebRTC/streaming-transcode backend anywhere in this repository) remains genuinely unresolved — Engine 10's scope was instrumentation only, per its Implementation Contract items 4/5. **Engine 11 (Video Interpreter Coordinator) — Phase 0 (Repository
Verification) and Phase 1 (Compose) both complete this pass**
(`docs/history/M388-E11-VideoInterpreterCoordinator-Compose.md`).
Anti-duplication scan clean — no existing video-interpreter/coordinator/
orchestration capability found anywhere; `core/engines/media/
coordinator/` confirmed free. Draft 7-item Implementation Contract
recorded, not yet approved. **Phase 2 (Review/Approval) is next —
not started this pass.**
- MD-007, MD-008 — real, confirmed gaps, but **cannot be resolved within M388 at all** (Phase 2 Review found the original task explicitly lists "Licensing of translation/voice models" as Out of Scope) — not just deferred, structurally excluded from this milestone's contract. Remain High priority as a standing gap, owned by a future milestone.
- MD-017 — found during Engine 3's Phase 0, re-confirmed (fresh repository-wide `registerSubsystem(` search, not just re-read) during Engine 3's Phase 2 Review (`docs/history/M388-E3-Translation-Compose.md`). `cozy-live.js`'s `relaySpeechSegment()` requires both `'CozyTranslate'` and `'CozySpeech'` subsystems via `getSubsystemOrThrow()` (mandatory, not optional like Engine 2's `'CozyLanguage'` hook) — zero production registrants exist for either (only Engine 2's own `'CozyLanguage'` registrant exists in production). The live pipeline cannot complete a call today. **Update, Engine 3 Phase 3 (Implementation, this pass):** `core/engines/media/translation/translation-pipeline-engine.js` built and registered into `core/bridge/engine-bridge-bootstrap.js`'s `REGISTRATIONS`; its `attachToLive()` registers a real `'CozyTranslate'` adapter via `cozy-live.js`'s own public `registerSubsystem()` API, per the Final Implementation Contract exactly as written (no locked file touched). 12/12 real, executed tests pass (`core/engines/media/translation/tests/translation-pipeline-engine.test.js`), including confirming the registered adapter's `translate()` returns exactly `{ text }` and never throws. The `'CozyTranslate'` half of `MD-017` is now resolved; the `'CozySpeech'` half remains unassigned, same status as `MD-016`.
- MD-020 — found during Engine 7's Phase 0/Compose (`docs/history/M388-E7-VoiceGeneration-Compose.md`), re-confirmed unchanged at Phase 2 Review (repo-wide search for `MediaStreamAudioDestinationNode`/`createMediaStreamDestination`/`getDisplayMedia` found no audio-capture path from `speechSynthesis`). **Engine 7 itself — Phase 0 through Phase 9 all now complete, CLOSED this pass.** `core/modules/speech/generation/voice-generation-engine.js` (orchestration-only, new file) composes `window.CozyOS.VoiceManager.speak()`, falling back to `window.CozyOS.CozyTTSBrowserAdapter.speakPreview()` only if `VoiceManager` is unavailable — neither existing file modified (confirmed unchanged this pass). **13/13 real tests pass** (`core/modules/speech/generation/tests/voice-generation-engine.test.js`); all 6 prior engines' suites re-run unmodified, 129/129 pass (142/142 total this pass, zero regressions). MD-020's own underlying question — no way to capture synthesized speech as a real audio buffer/track — remains genuinely unresolved; Engine 7's scope was orchestration only, per its own Implementation Contract item 6 (must not attempt Option B/C). Still blocks Engine 9 (Media Encode)'s actual output — carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016`/`MD-018`.
- MD-021 — new at Engine 8's Phase 0/Compose (`docs/history/M388-E8-Synchronization-Compose.md`). Direct confirmation that no engine in the Approved 11-engine order produces a real audio duration or buffer (Engine 1's decode: structural-only `isReal:false`; Engine 7's speech generation: playback-only, `realAudioBuffer:false` in every code path) — so no component can compute a real numeric timing offset/drift between generated speech and the original video. Not an Engine 6 or Engine 7 defect — an environment-level constraint discovered at Engine 8's Compose. **Engine 8 itself — Phase 0 through Phase 4 now complete this pass.** `core/engines/media/synchronization/synchronization-engine.js` (new file) implements the honest scope confirmed at Phase 2 Review: a real, deterministic `crossCheckTiming()` classification (`aligned`/`timing-without-playback`/`playback-without-timing`/`unresolved`) — never a fabricated drift value. **21/21 unit tests + 3/3 real integration tests pass** (against the actual `subtitle-timeline-engine.js`/`voice-generation-engine.js`, not fixtures); all 7 prior engines' suites re-run unmodified and pass (the one pre-existing `media-pipeline-manager.test.js` failure is `MD-004`/`MD-009`, confirmed identical in the pristine, unmodified checkout — not a regression). MD-021's own underlying question — no way to compute a real drift number — remains genuinely unresolved; Engine 8's scope was cross-check classification only, per its Implementation Contract item 5. Still blocks any future "true" audio-timing synchronization, same relationship `MD-020` has to Engine 9.
- MD-022 — found during Engine 8's Phase 2 Review while re-verifying `AA-008`. Informational/tangential, not this engine's scope — unaffected by Engine 8's Implementation. **Phrasing corrected this pass (`DI-010`, Engine 11 Phase 0/1): a module named "Scene Manager" does exist (`core/engines/scene/scene-manager.js`), but its real, verified scope (production camera/audio scene switching) does not include the frame-level export-synchronization capability `record-export-session-manager.js`'s docstring gestures at — MD-022's substantive conclusion stands, only its literal wording was inaccurate.** See full detail above and in `docs/history/M388-E8-Synchronization-Compose.md` / `docs/history/M388-E11-VideoInterpreterCoordinator-Compose.md`.

**Medium:**
- MD-004 — low operational risk (already fails closed), deferred pending a dedicated feature-scope pass.
- MD-005 — same treatment as MD-004; discovered only after AA-004's fix let the relevant code path execute for the first time.
- MD-010 — Engine 5's Phase 0–2 all complete (`docs/history/M388-E5-BackgroundAudioSeparation-Compose.md`) — Approved (not Revised) at Phase 2 Review. **Phase 3 (Implementation) done this pass**: `core/engines/media/audio-separation/background-audio-separation-engine.js` + `provider-turn-coverage.js` (new files only, deliberately at a path distinct from the unrelated, still-unbuilt visual `background-engine.js` — see `AA-007`). Real, deterministic partition of segments into `speech` (covered by a real Engine 4 diarization turn) vs. `unclassified` (never `background` — no positive signal supports that inference) when diarization turn data is supplied; an honest `isReal:false`/`method:'no-analyzable-signal'` empty envelope otherwise. **18/18 real tests pass** (`core/engines/media/audio-separation/tests/background-audio-separation-engine.test.js`), including a real end-to-end composition test against Engine 4's own `diarize()` output (no mocking). Now 🔵 Implementing (Verification/Close — Phase 4–9 — not yet done this pass).
- MD-011 — Engine 4's Phase 0–2 all complete (`docs/history/M388-E4-Diarization-Compose.md`) — Approved (Revised) at Phase 2 Review. **Phase 3–9 all complete, Engine 4 CLOSED.** `core/engines/media/diarization/speaker-diarization-engine.js` + `provider-speaker-hint.js` (new files only, per the Final Implementation Contract — no locked file touched, no exception). Real, deterministic grouping of contiguous caller-supplied speaker hints into turns (`diarizeSegments()`); results are written into `cozy-speech.js`'s existing `_speakers` registry only through its already-public `registerSpeaker()`/`addActiveSpeaker()` methods (`applyToSpeechRegistry()`). With no hint on any segment, an honest `isReal:false`/`method:'no-analyzable-signal'` empty envelope is returned — no fabricated speaker count or turn boundary, since no acoustic diarization model exists in this environment (same constraint as Engine 1's `isReal:false` audio-track envelope). **23/23 real tests pass** (`core/engines/media/diarization/tests/speaker-diarization-engine.test.js`). Engine 4 itself is Closed (Phase 9); `MD-011` remains 🔵 Implementing as a Repair Queue entry because it tracks the capability, not the engine's own lifecycle — now consumed as a real upstream signal by Engine 5 (see `MD-010`, above).
- MD-019 — found during Engine 4's Compose (Phase 1), decided at Engine 4's Phase 2 Review: no exception granted to touch `cozy-live.js`; Engine 4 revised to stay fully external. Remains open/unassigned, same treatment `MD-016` got.
- MD-012 — Engine 2's Phase 0/Compose/Phase 2 Review all complete (`docs/history/M388-E2-LanguageDetection-Compose.md`). **Phase 3 (Implementation) done this pass**: `core/engines/media/language/language-detection-engine.js` + `provider-lexical.js` (new files, per the unrevised Implementation Contract). Real Unicode-script classification (Ethiopic → Amharic) and a real, deliberately-partial curated lexical-overlap heuristic (en/fr/sw/so/ha/yo/zu/lg) run only when text is actually available for a segment (explicit `hintText` or a duck-typed property on the opaque `audioRef`); otherwise an honest `isReal:false` empty envelope is returned — no fabricated acoustic detection, since no such model exists in this environment. Registered into `cozy-live.js`'s existing `CozyLanguage` subsystem slot only via `registerSubsystem()` (`attachToLive()`), never by editing `cozy-live.js` itself. **31/31 real tests pass** (`core/engines/media/language/tests/language-detection-engine.test.js`). Now 🔵 Implementing (Verification/Close — Phase 4-9 — not yet done this pass).
- MD-016 — found during Engine 1's Phase 2 Review; a real milestone-level sequencing gap (no owner yet for the audio-buffer→STT bridge), not an Engine 1 defect. Checked again during Engine 2's Phase 0, and again during Engine 3's Phase 0 — confirmed adjacent but non-blocking each time.
- MD-018 — found during Engine 3's Phase 0, resolution path decided during Engine 3's Phase 2 Review (`docs/history/M388-E3-Translation-Compose.md`). `relaySpeechSegment` computes `detectedLanguage` (via Engine 2's optional `'CozyLanguage'` hook) but never forwards it into the `'CozyTranslate'` adapter's `translate()` call — a real gap inside `cozy-live.js`'s own already-shipped pipeline logic (the `sourceLanguage` argument passed to `translate()` is hardcoded to `session.primaryLanguage`, not reachable by an external adapter). **Decided this Review:** fixing it requires editing `relaySpeechSegment()` itself, which Engine 3's Implementation Contract forbids — no exception granted; remains open and unassigned, carried forward as its own future fix, not Engine 3's scope.

**Low:**
- MD-014 — Engine 6's Phase 0–2 all complete (`docs/history/M388-E6-SubtitleTimeline-Compose.md`) — Approved (not Revised) at Phase 2 Review. **Phase 3 (Implementation) done this pass**: `core/engines/media/subtitles/subtitle-timeline-engine.js` + `provider-srt-formatter.js` (new files only — no locked file touched, confirmed via `diff -rq` against the Engine-5-closed baseline). Real, deterministic cue-timeline construction (sorting, sequential cue numbering, real overlap detection) and `.srt` export, only from caller-supplied segments that already carry real text and real millisecond timing; a segment missing either is honestly skipped and reported in `skippedSegmentIds`, never fabricated — this engine has no transcription, translation, or timing-inference capability of its own. **22/22 real tests pass** (`core/engines/media/subtitles/tests/subtitle-timeline-engine.test.js`; one test-file bug — sorting a frozen array in place — was caught and fixed during Phase 4's verify-fix-reverify loop, not an engine defect). Now 🔵 Implementing (Verification/Close — Phase 4–9 — not yet done this pass).
- MD-015 — lip-sync capability confirmed absent repository-wide, explicitly Out of Scope this Compose per the task itself. Unaffected by Engine 6.
- DI-004 — `core/language.js:32` dead-reference to an unassigned `window.CozyLanguage` global, discovered during Engine 2's Phase 0 repository-wide search. Pre-existing, unrelated to M388, not fixed this pass (out of this session's explicit no-fixes scope). Full detail: `docs/builder/knowledge/documentation-integrity-registry.md`.

**Resolved this pass:**
- DI-011 — found during Engine 11's own Phase 4 Verification (independent
  re-read, not a repeat of the prior round's claims): `LATEST.md` and
  `HANDOFF.md` each carried a second, separate "Current Engine" status
  block (further into each file, distinct from the top-of-file summary)
  that still read "11 (Phase 0–1 of 9 complete this pass)" — stale from
  Round 15, missed when Round 16's top-of-file summaries were written.
  Corrected in both files this pass to "Phase 0-3 of 9 complete this
  pass." No code affected; documentation-only.
- MD-023 — found during Engine 11's own Phase 2 Review: `translateSegment()`
  (Engine 3) requires a live `speechTranslationAdapter` instance as its
  first argument and fails closed without one — not called out explicitly
  in Engine 11's Draft Contract. Folded into the approved contract:
  the coordinator accepts it as an optional param and honestly skips
  Translation when absent, recording the skip. No engine's own code
  changed; resolved at the coordinator's call-site only.
- MD-024 — found during Engine 11's own Phase 3 Implementation: Engine 9's
  `buildEncodePlan()` hard-requires an array for `speechResults` and a
  real `{results:[...]}` object for `syncResult` — throws `TypeError` on
  `null`, so it cannot honestly run when Engine 8 (Synchronization) was
  skipped. Coordinator corrected to cascade the skip (Encode now honestly
  skipped whenever Synchronization is), matching the real 7 → 8 → 9
  dependency chain in code, not just in prose. **10/10 real Engine 11
  tests pass** (`core/engines/media/coordinator/tests/video-interpreter-coordinator.test.js`),
  including a dedicated test for this cascade.
- DI-005 — the documented repository-hashing method mis-splits the three filenames in this repository that contain spaces; this was the real, previously-unidentified root cause of the Round 10/11/12 SHA-256 discrepancy earlier rounds correctly logged as open rather than guessed at. Canonical method corrected (`-print0`/`-z`/`-0`), reproducibly confirms Round 12's own recorded hash. Full detail: `docs/builder/knowledge/documentation-integrity-registry.md`.
- DI-008 — backfilled this pass. A real finding from the Engine 10 Phase 3 round (`LATEST.md`'s stale top summary, corrected that round) was referenced by id in three files but never actually given its own Repair Queue row, a Rule 62 process gap. Row added retroactively at Engine 10's Close round; no further action needed, the underlying `LATEST.md` staleness was already fixed when found.
- DI-009 — Engine 10 Close round's own Phase 0 re-verification found the incoming ZIP's claimed Repository SHA-256 did not match the hash independently computed from its actual extracted contents (confirmed reproducible, not a locale/sort artifact). Per Rule 69, the independently-verified hash (`92adfd8e...`) was adopted as this round's authoritative starting state rather than the claimed one; Engine 10's own implementation/tests were not affected (Phase 3/4 work and its 199/199 test result predate and are independent of this hash question). Root cause of the original discrepancy not determined this pass — flagged for any future session that can trace it.

**Open — still pending (RP-026 was an explicitly-directed independent path this pass; it does not resolve this item):**
- RP-025-A Live Verification — real on-device model availability for `core/modules/intelligence/providers/on-device-conversational-provider.js` cannot be observed from this repository/session (no browser runtime here). Next session's entire scope: FIND (check `LanguageModel`/`window.ai.languageModel` on the actual Android Chrome deployment, via dev console) → FIX (only if the phone demonstrates an actual capability-detection/model-loading defect — no unrelated changes) → RECORD (the real observed browser state, verbatim, as its own entry in `docs/builder/knowledge/repair-history-registry.md`). `NOT_READY`/`MODEL_NOT_INSTALLED` is an honest, acceptable result, not automatically a bug — Prompt API support is currently Chrome-desktop/origin-trial-centric, with little-to-no Android Chrome coverage and none on iOS. Full spec: `HANDOFF.md`'s CONTINUATION POINT. **Note:** the Assistant's user-visible symptom this item was originally chasing (no genuine reply) is now separately resolved by RP-026 (rule-based composer, active) — this item remains open only for the on-device provider's own real-runtime status, not as a blocker to the Assistant's usability.

**Open — carried forward, unaffected by RP-027:**
- (No new open item added by RP-027 itself — see "Resolved this pass" below. `RP-025-A Live Verification`, listed above, remains the only standing open repair item.)

**Resolved this pass (RP-027 — CozyOS Conversational Knowledge + Multilingual Response Expansion, single-path, owner-directed, IMPLEMENTED + TESTED + VERIFIED + PACKAGED):**
- RP-027 — extended `rule-based-conversational-provider.js`'s `INTENT_RULES`/`composeReply()` from RP-026's 7 intents to 20, adding CozyOS-identity (`founder`, `what-is-cozyos`, `what-is-cozyos-enterprise`), `list-apps`, `how-to-register`, `how-authentication-works`, `phone-verification`, `account-status`, `what-is-provider`, `list-providers`, `provider-not-ready`, and `control-center`. Each evidence-backed intent (`founder`/`list-apps`/`list-providers`) is graded VERIFIED / PARTIALLY_VERIFIED / NOT_FOUND against real repository/runtime evidence via new file `core/modules/intelligence/knowledge/cozy-knowledge-registry.js` (reads `DeveloperIdentity`, `ServiceRegistry`, `ProviderManager`, `LivingAI`'s already-existing public APIs at call time, never load time; a missing/throwing dependency always degrades to honest `NOT_FOUND`, never a fabricated answer). New files `core/modules/intelligence/language/cozy-language-registry.js` and `cozy-language-templates.js` add a 5-language default registry (English/Kiswahili/French/Arabic/Somali, all `AVAILABLE` with verified, committed templates) and a 6-language extended registry (Luo/Kikuyu/Kikamba/isiZulu/Luganda/Igbo, honestly held `NOT_READY` this pass — no verified templates exist for them yet, a disclosed gap, not an omission); `resolveLanguage()` implements manual-selection > requested > country-suggestion > English precedence, always returns an `AVAILABLE` code, and reports `fallback:true`/`reason` whenever the resolved language differs from what was actually asked for — never a silent substitution. `index.html`/`dashboard.html` each got three additive `<script>` tags; RP-026's own `LivingAI.registerProvider()`/explicit `setActiveProvider()` architecture is unchanged in logic. **92 new tests** (language registry 15/15, knowledge registry 11/11, provider RP-027 matrix 66/66 — including a full intent×default-language matrix, Fact Safety VERIFIED-vs-honest-fallback checks, unknown-question honesty checks, and all 6 extended languages individually confirmed to honestly fall back rather than fabricate a translation) plus RP-024 (10/10), RP-025-A (8/8), and RP-026 (14/14) regression re-confirmed unchanged — **124/124 total, zero regressions.** Locked files (`core/living/cozy-living-assistant.js`, `core/modules/cognitive/cognitive-coordinator.js`, `core/modules/intelligence/cozy-intelligence-provider.js`, `core/config.js`) confirmed untouched. Repository SHA-256: `cf6fb2e3b3688706bc7839bfe321ee21c556ddf135e51d42d4ed656eabe82358`. Package: `CozyOS-main-RP-027.zip`, SHA-256: `f9be60a712bcedb196652ce5c2da9045dfc7de184cbb71b0f5d6626fd5445937`. **Open continuation point (not a new Repair Queue item, tracked in HANDOFF.md's own CONTINUATION POINT):** the 6 extended languages need verified, in-language-reviewed templates before their registry state can honestly move to `AVAILABLE`. Full detail: `docs/builder/knowledge/repair-history-registry.md`.

**Resolved this pass (RP-028 — Luo Language Availability, single-path, owner-directed, VERIFIED NOT_READY — no promotion, no fabrication):**
- RP-028 — real FIND/VERIFY pass on Luo (Dholuo), RP-027's own extended language. FIND confirmed Luo registered `NOT_READY` with zero existing template entries — a clean-slate check, not a continuation of partial work. VERIFY located real dictionary/phrasebook sourcing for basic Dholuo greeting/thanks vocabulary but found genuine, unresolved disagreement between independent sources even at that level (e.g. whether "Misawa" is a general or afternoon-specific greeting), and found **zero** authoritative sourcing anywhere for CozyOS's ~20 technical intents (provider status, `NOT_READY` explanations, account states, registration/authentication, etc.) — only paid commercial translation-agency services exist for that depth, not verifiable in-session. Checked against all 5 Rule 82 conditions: real language resources (⚠️ partial, disputed), templates for every intent (❌), no uncontrolled machine translation (N/A — none written), passing intent×language tests (❌ N/A), observed runtime behavior (❌ no browser/DOM runtime available). **Per this repair's own Critical Rule, Luo remains `NOT_READY` — no Luo template was added, no registry state changed.** FIX made zero changes to `cozy-language-registry.js`, `cozy-language-templates.js`, or `rule-based-conversational-provider.js` (all three confirmed byte-identical to their RP-027-shipped hashes). TEST added new file `core/modules/intelligence/language/tests/rp-028-luo-availability.test.js` — **15/15 new tests**, all behavioral (registry state, `isAvailable`, fallback disclosure for both manual and requested Luo selection, country-suggestion table never mapping to Luo — including for real Luo-speaking regions Kenya/Tanzania/Uganda/South Sudan, six representative intents each honestly falling back when requested in Luo, and confirmation that requesting Luo never throws). Full regression re-confirmed unchanged: RP-024 (10/10), RP-025-A (8/8), RP-026 (14/14), RP-027 language registry (15/15), knowledge registry (11/11), provider matrix (66/66) — **139/139 total, zero regressions.** Locked files and RP-027's own three source files confirmed byte-identical (unchanged hashes recorded). Full detail: `docs/builder/knowledge/repair-history-registry.md`.


- RP-026 — real rule-based conversational provider implemented (`core/modules/intelligence/providers/rule-based-conversational-provider.js`), registered into `LivingAI`'s existing provider registry under a new `"rule-based-conversational"` slot and (optionally) `ProviderManager`, then explicitly activated via `setActiveProvider()` as a disclosed, separate step. Classifies raw input against a small, disclosed intent set (greeting-morning/afternoon/evening/generic, thanks, identity, help) and returns an honest `.text` reply for each, plus an honest "not supported yet" reply otherwise — never LLM, never fabricated understanding, never pipeline internals surfaced as an answer. Calls `CognitiveCoordinator.run()` first so Memory/Policy still genuinely execute. 14/14 new tests pass; RP-024 (10/10) and RP-025-A (8/8) regression suites both unaffected. Full detail: `docs/builder/knowledge/repair-history-registry.md`.

**Resolved prior pass (post-M388 free-account repair session):**
- RP-025-A — real on-device conversational provider implemented (`core/modules/intelligence/providers/on-device-conversational-provider.js`), registered into `LivingAI`'s existing `"on-device"` slot and (optionally) `ProviderManager`. Honestly reports `NOT_READY`/`MODEL_NOT_INSTALLED`/`READY` based on live browser feature detection — never fabricates an ONLINE state. No auto-activation; `setActiveProvider()` remains the sole choke point. 8/8 new tests pass; RP-024 regression suite unaffected. Full detail: `docs/builder/knowledge/repair-history-registry.md`.

## Not Yet Composed (known future work, no Repair Queue entry yet — listed here only so it isn't lost, not a substitute for a real Compose stage)

- **Language Expansion Roadmap (owner-provided, this pass; not yet
  Composed as a numbered repair)** — Confirmed against the actual
  `cozy-language-registry.js` shipped in `CozyOS-main-RP-027.zip`: the
  5 defaults (`en`→`sw`→`fr`→`ar`→`so`, country-suggested + manually
  selectable, exactly as intended) are `AVAILABLE`; the 6 languages
  RP-027 registered as extended (Luo, Kikuyu, Kikamba, isiZulu,
  Luganda, Igbo) are honestly `NOT_READY` — no verified templates
  exist for any of them, and the registry does not pretend otherwise.
  The owner has supplied a substantially larger candidate list for a
  **future** extended-language pass (explicitly NOT RP-027 continued —
  a new, separately-numbered repair when Composed). Recorded here
  verbatim as the authoritative starting point, priority as given by
  the owner:
  - 🔴 Very high: Hausa, Yorùbá, Oromo, Amharic, Fulfulde/Fula, Igbo,
    Nigerian Pidgin
  - 🟠 High: Shona, Akan/Twi, Lingala, Wolof, Bambara, Tigrinya,
    Kinyarwanda, Kirundi, Xhosa, Afrikaans, Portuguese (also flagged
    🟠 High for Madagascar: Malagasy)
  - 🟡 Medium/regional/country priority: Sesotho, Setswana,
    Chichewa/Chewa, Ndebele, Tsonga/Xitsonga, Swati, Sango, Kanuri,
    Mossi/Mooré, Dyula/Jula, Ewe
  - Also named for extended-registry inclusion alongside the original
    6: Lingala, Afrikaans, Portuguese (Lingala and Portuguese also
    appear in the prioritized table above; listed once here for the
    registry, not twice for priority).
  **Not started** — no registry/template code changed by this note.
  **Governed by Rule 82 (`docs/builder/rules/27-language-availability-
  verification-rule.md`, adopted this pass):** every new language code
  is added to `cozy-language-registry.js` at `NOT_READY` first, and may
  only be flipped to `AVAILABLE` after real language resources are
  identified, verified templates exist in `cozy-language-templates.js`
  for every supported intent, no uncontrolled machine translation was
  used, that language's own intent×language tests are written and
  pass, and runtime behavior is actually observed (or honestly recorded
  `NOT_TESTED_LIVE` if no browser/DOM runtime is available) — all five,
  independently, in the same session that changes the registry state.
  Appearing on this roadmap, or carrying a 🔴/🟠/🟡 priority marking, is
  never itself grounds for promotion; priority affects work order only.
- **See also Rule 83** (`docs/builder/rules/28-universal-builder-and-
  public-knowledge-governance-rule.md`) — the owner's separately
  supplied language target list is **resolved as 17 default targets**:
  English, Kiswahili, French, Arabic, Somali, Russian, Chinese/
  Mandarin, Hausa, Yorùbá, Luo, Kikuyu, Kikamba, isiZulu. Somali is
  preserved (RP-027-verified, `AVAILABLE`, not dropped); Russian/
  Chinese/Mandarin are intentional (Africa-first now includes broader
  global accessibility); Luo/Kikuyu/Kikamba/isiZulu remain `NOT_READY`
  until each independently satisfies Rule 82 — target-list membership
  is never itself evidence of readiness. Russian/Chinese/Hausa/Yorùbá
  are not yet registered even as `NOT_READY` placeholders — open for a
  future session.
- - **See also Rule 84** (`docs/builder/rules/29-language-taxonomy-and-
  single-source-governance-rule.md`) — owner-directed, policy-only,
  eight structural additions required before the next language
  implementation: (1) Target/Registered/Available kept as three
  independent fields, never `TARGET → AVAILABLE` directly; (2)
  country/region mapping stored as many-to-many metadata, never
  country = language; (3) dialect/variant metadata recorded on the
  parent language, not premature new registry entries; (4) `script`/
  `direction`/`locale` as first-class registry fields (Arabic/RTL
  named specifically); (5) offline-resource state (templates/extended
  pack/audio pack/knowledge pack/online expansion) tracked
  independently of Rule 82 conversational `AVAILABLE`; (6) voice
  (text→speech/speech→text/conversational round-trip) verified
  separately from text templates; (7) public-answer facts (founder,
  vision, ownership, etc.) authored once in a public knowledge source
  and rendered per language, never re-authored per language; (8) a
  bounded CozyOS Public Story kept distinct from internal Builder/
  Governance information. Governing principle: facts have one
  authoritative source, languages only render it. **No registry or
  template code changed by this note** — open for a future session to
  Compose explicitly, exactly like the Rule 82 roadmap item above.
Interactive auth-flow verification (registration, login, logout,
  remember-me, reload, session restore, OTP, recovery codes, trusted
  device) — has not been Composed yet; no finding exists until real browser
  testing actually surfaces one.
- Mobile/Android verification — same; not yet Composed.
- `LivingDecisionEngine.recordOutcome()` — carried over from M387 as a known
  gap, not yet formally Composed as its own finding.

| RP-029-A (Document/Website/Community-Submission Knowledge Ingestion Pipeline) | 🟢 Fixed | — (closed, scope as delivered) | RP-029-A session | None |
| RP-029-B (Community Contribution + Knowledge Validation) | 🟢 Fixed | — (closed, scope as delivered: review workflow, source-aware independence check, labeled confidence, privacy/pseudonymization, Rule 82 read-only reporter, offline-sync data model only) | RP-029-B session | Admin UI for the review workflow (begin review/confirm/dispute/reject/promote) does not exist yet — API only |
| RP-029-C Phase 1 (Community Review & Validation Interface — Data/State Layer) | 🟢 Fixed | — (closed, scope as delivered: partialConfirm/requestClarification reviewer actions, audited challenge/confirm/reject/promote wrappers, derived display-state mapper, full five-part Rule 82 gate, own audit trail — no UI) | RP-029-C Phase 1 session | Dashboard/contribution UI does not exist yet — API only. Rule 82 gate trusts caller-supplied test evidence rather than running a suite itself. |
| RP-029-C Phase 2 (Review Dashboard UI + Living Engines/Cozy Offline Hotspot reuse requirement) | 🟢 Fixed | — (closed, scope as delivered: real browser dashboard, authorization-guarded actions, logic-level Rule 82 promotion gate, Cozy Offline Hotspot composition for share/receive) | RP-029-C Phase 2 session | Contribution-submission screen does not exist yet. REVIEWER role is an allowlist, not a real base-auth role. Hotspot sharing is manual-pairing only, no auto-discovery/relay. Other four inspected Living Engines not yet composed into this domain. |
| RP-029-C Phase 3 (Community Contribution Interface) | 🟢 Fixed | — (closed, scope as delivered: oral-language-first contribution form, consent-gated submission, real registry-backed language list, honest Cozy Offline Hotspot sharing) | RP-029-C Phase 3 session | No admin-dashboard contribution analytics yet. DOCUMENT_EVIDENCE/WEBSITE_EVIDENCE/OCR_TEXT are metadata-only — no real OCR/fetch backend exists. Post-submission withdrawal is CAPABILITY_UNAVAILABLE. SYNCED/CONFLICT timeline states remain unreachable (no real sync/merge engine). |
| RP-029-C Phase 4 (Mandatory Content Safety Gate) | 🟢 Fixed | — (closed, scope as delivered: SAFE/UNSAFE/UNCERTAIN text classification gating both local submission and offline hotspot receipt; quarantine store for human review) | RP-029-C Phase 4 session | No admin-facing quarantine review UI yet — listQuarantined()/releaseFromQuarantine() are real but unwired. Heuristic is intentionally small/generic; sexual-content-involving-minors and extremist-recruitment categories are not keyword-matched at all (disclosed — routed to quarantine on any adjacent signal). |
| RP-029-C Phase 5 (Quarantine + Admin Safety Review) | 🟢 Fixed | — (closed, scope as delivered: full quarantine state machine, REVIEWER+-gated release/reject/escalate, append-only audit trail, real admin dashboard page, HIGH_RISK tier + dedup added to the safety gate) | RP-029-C Phase 5 session | analytics() is current-contents-only, no historical totals. Escalation has no specialized backend to hand off to. REVIEWER/ADMIN remain dashboard-local designations, not production identity management. |
| MD-025 (`core/engines/audio/test/audio-manager.test.js` — 15/15 failing under plain `node`, discovered incidentally during RP-029-A's full-suite regression re-run, not investigated) | 🟡 Composed | Medium | Future Builder | Root cause not yet diagnosed — could be a real regression or an environment/dependency gap; needs its own FIND pass before any fix is attempted |
| MD-026 (`core/bridge/test/engine-bridge.test.js` — 1 failure, discovered incidentally during RP-029-A's full-suite regression re-run, not investigated) | 🟡 Composed | Low | Future Builder | Same caveat as MD-025 — not yet diagnosed |
| MD-027 (`scene-manager.test.js`, `media-pipeline-manager.test.js`, `playback-engine.test.js`, `camera-manager.test.js` x2, `media-integration.test.js` all crash outright under plain `node` rather than reporting pass/fail counts, discovered incidentally during RP-029-A's full-suite regression re-run) | 🟡 Composed | Medium | Future Builder | Likely a browser/DOM-API dependency (e.g. `document`, `navigator.mediaDevices`) unavailable under plain Node — needs confirmation, not assumed |
| RP-033-GATE1 (Cozy Living Offline Connectivity — Gate 1: Connectivity Core + Capability Detection; new `core/connectivity/cozy-living-connectivity.js` composes the existing, unmodified `cozy-connect.js` and `live-hotspot-engine.js` for honest six-value capability status reporting, defines the offline-first connectivity state machine with an enforced invalid-transition guard, the store-and-forward Cozy packet contract, and identity/session/invitation/replay-protection contracts; native Wi-Fi Direct and native OS-level hotspot creation honestly `REQUIRES_NATIVE_COMPANION`, never fabricated as available) | 🟢 Delivered (34/34 tests passing; `CozyOS-main-RP-033-Gate1.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | `cozy-connect.js`, `live-hotspot-engine.js` (both composed, not modified) |
| RP-033-GATE2-TRANSPORT (real QR/manual pairing + real WebRTC transport wired end-to-end through the Gate 1 coordinator; `cozy-connectivity-transport.js` now actually invokes `LiveHotspotEngine.createHost()`/`joinHost()`/`completeHostPairing()`, real DataChannel send/receive, packet-integrity pipeline, offline queue) | 🟢 Delivered (51/51 new tests + 34/34 Gate 1 regression passing; genuine Chromium/Playwright E2E attempted, ICE-gathering-complete environment-blocked in this sandbox, honestly reported `ATTEMPTED, PARTIAL`; `CozyOS-main-RP-033-Gate2.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | `cozy-connect.js`, `live-hotspot-engine.js`, `cozy-living-connectivity.js`, `cozy-share.js` (all composed, not modified) |
| RP-033-BLE-TRANSPORT (real BLE GATT read/write data-transport protocol over `CozyConnect.bluetooth`'s already-real device selection/pairing; Gate 2 composes detection/scan/connect only, no GATT protocol implemented) | 🟡 Composed | Medium | Future Builder | RP-033-GATE1, RP-033-GATE2-TRANSPORT |
| RP-033-STORE-AND-FORWARD-RELAY (actual multi-hop packet relay/routing using the Gate 1 packet contract; wiring into the real `core/network/cozy-network-orchestrator.js` router, which is ES-module-flavored and was deliberately not bridged this pass) | 🟡 Composed | High | Future Builder | RP-033-GATE1 |
| RP-033-CRYPTO-SETTLEMENT (transaction/payment message support over the transport-agnostic Gate 1 packet contract; explicitly out of scope for Gate 1 per its own build prompt) | 🟡 Composed | Medium | Future Builder | RP-033-GATE1, RP-033-GATE2-TRANSPORT |
| RP-033-TRUST-EVALUATION (full trust evaluation for paired devices, composing `living-security-coordinator.js`/`living-trust-engine.js`; Gate 1 only prepares identity/session/challenge-response *contracts*, not evaluation logic) | 🟡 Composed | Medium | Future Builder | RP-033-GATE1 |
| RP-033-COZY-SHARE-INTEGRATION (wiring the Gate 1 connectivity coordinator into `core/collaboration/cozy-share.js`'s existing session/role/trust layer, where appropriate) | 🟡 Composed | Low | Future Builder | RP-033-GATE1 |
| RP-034-PHASE1-CONNECTOR (Remote Media Intelligence Connector Foundation — YouTube; new `core/modules/intelligence/media/cozy-media-connector.js`: generic `MediaConnectorRegistry`, real `YouTubeConnector` with authorization-state tracking, honest capability detection reusing RP-033's vocabulary, real URL/duration parsing, real YouTube Data API v3 metadata retrieval; download/frame/transcript/OCR permanently `CAPABILITY_UNAVAILABLE` by design) | 🟢 Delivered (30/30 new tests + 202 regression tests passing; `CozyOS-main-RP-034-Phase1.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | `cozy-knowledge-ingestion.js`, `cozy-knowledge-safety-gate.js`, `cozy-language-pack-registry.js`, `cozy-language-acquisition-pipeline.js` (all located/read, none modified or called yet) |
| RP-034-PHASE2-INDEX (persistent Remote Media Intelligence Index — real CRUD/versioning via composed `CozyMemory`; real duplicate prevention; real field-aware search; real per-field provenance; real language routing composing RP-030 read-only; real privacy-secret rejection; honest `SYNC_CAPABILITY_UNAVAILABLE`-only sync contract; real `refreshMetadata()` composing the real Phase 1 connector; no download/frame/OCR/transcript capability) | 🟢 Delivered (55/55 new tests + 54 pre-existing suites passing; `CozyOS-main-RP-034-Phase2.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE1-CONNECTOR, `cozy-memory-engine.js`, RP-030, RP-029-C (all composed, none modified) |
| RP-034-INDEX-DISK-PERSISTENCE (`CozyMemory`, the real persistence primitive Phase 2 composes, is real, in-memory, session-scoped storage only — no disk/IndexedDB-backed engine exists anywhere in this repository; "persistent" in Phase 2's own file header means "real CRUD/versioning/search across calls within the running session," not "survives a page reload") | 🟡 Composed | Medium | Future Builder (only if page-reload persistence is actually required) | RP-034-PHASE2-INDEX, `cozy-memory-engine.js` |
| RP-034-PHASE3-SEARCH (Remote Media Search & Research Engine — real, deterministic search/ranking over the real Phase 2 index; matchType priority EXACT_TERM/EXACT_PHRASE/LANGUAGE/DIALECT/REGION/METADATA/PARTIAL, never a fabricated relevance score; real language routing for queries composing RP-030; real research aggregation/regional comparison/conflict detection; real term-frequency distinguishing SOURCE_FREQUENCY from user-usage telemetry; real refresh delegation to Phase 1/2; real quarantine visibility composing RP-029-C; no semantic search) | 🟢 Delivered (56/56 new tests + 55 pre-existing suites passing; `CozyOS-main-RP-034-Phase3.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE2-INDEX, RP-030, RP-029-C (all composed, none modified) |
| RP-034-PHASE4-PIPELINE (Full Remote Media Intelligence Pipeline — real 9-job-type coordinator over the real Phase 1-3 chain; real four-level language routing composing RP-030 with honest LANGUAGE_UNCERTAIN/AMBIGUOUS_LANGUAGE outcomes; real safety-gate integration via RP-029-C before any extracted term becomes knowledge; real 7-domain knowledge separation, always caller-asserted, never auto-verified; real hotspot transport composing RP-033 Gate 2's real sendPacket/receivePacket with real duplicate/safety/audit checks on receive; TRANSCRIPT_ANALYSIS/TERM_EXTRACTION/PHRASE_EXTRACTION real only with caller-supplied text; TOPIC_EXTRACTION and automatic LANGUAGE_IDENTIFICATION permanently CAPABILITY_UNAVAILABLE — no real backend exists) | 🟢 Delivered (63/63 new tests + 56 pre-existing suites passing; `CozyOS-main-RP-034-Phase4.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE1-CONNECTOR, RP-034-PHASE2-INDEX, RP-034-PHASE3-SEARCH, RP-029-A, RP-029-C, RP-030, RP-033-GATE2 (all composed, none modified) |
| RP-034-PHASE4-LANGUAGE-ID-BACKEND (`core/engines/media/language/language-detection-engine.js`, M388 Engine 2, is a real text-based script/lexical language-hint engine but an ES module — a different module system from Phase 4's entire CommonJS/window.CozyOS composed chain; Phase 4's `LANGUAGE_IDENTIFICATION` job was honestly scoped to explicit, RP-030-verified evidence rather than automatic text-based detection, to avoid the real integration risk under this repair's own delivery timeline) | 🟡 Composed | Low | Future Builder (only if cross-module-system composition is worth the integration cost) | RP-034-PHASE4-PIPELINE, `language-detection-engine.js` |
| RP-034-PHASE5-AFRICAN-LANGUAGE-ROUTING (African Language Intelligence & Automatic Pack Routing — real six-level routing hierarchy (community+dialect → community → region → country → general pack → honest LANGUAGE_UNCERTAIN/NO_PACK fallback) over the real RP-030 registry; real evidence-hierarchy-derived confidence; real meaning isolation across communities; real ASR-readiness interface with no fabricated transcription; real code-switching/multi-language-conversation support; composes RP-031 teaching pipeline, RP-034 Phase 3/4, and RP-033 Gate 2 hotspot transport; Rule 82 fully untouched) | 🟢 Delivered (63/63 new tests + 57 pre-existing suites passing; `CozyOS-main-RP-034-Phase5.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE4-PIPELINE, RP-030, RP-031-PHASE2A, RP-029-C, RP-034-PHASE3-SEARCH, RP-033-GATE2 (all composed, none modified) |
| RP-034-PHASE5-COMMUNITY-FIELD (RP-030's real schema has no first-class "community" field distinct from region — Phase 5's community representation reuses RP-031's own real, established `region (community)` composite-string convention rather than adding new registry capability; a genuine, disclosed architectural limitation, not a bug) | 🟡 Composed | Low | Future Builder (only if a first-class community field is judged worth RP-030's own schema migration) | RP-034-PHASE5-AFRICAN-LANGUAGE-ROUTING, RP-030 |
| RP-034-PHASE6-PRIVACY-IDENTITY (Privacy, Identity & Provenance — 7 real separate identity types (device/user/contributor/source/knowledge/media-owner/reviewer, composing real RP-033 Gate 1/AuthCoordinator/RP-029-C role resolution, no competing role system); 6 real privacy tiers with tier-based display filtering; real expiring/revocable purpose-and-source-scoped consent; real sequential 6-stage knowledge lineage (never skips a stage); real redaction; real export/transfer/research/publish/share controls; real privacy-aware RP-033 packet filtering (TRANSFER_BLOCKED_PRIVACY, never fabricated SYNCED); real receiving-device validation (never a direct trusted-pack insert); real domain-knowledge protection; real append-only audit trail; honest right-to-withdraw with no real deletion mechanism, never claims "deleted everywhere") | 🟢 Delivered (108/108 new tests + 58 pre-existing suites passing; `CozyOS-main-RP-034-Phase6.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE1-CONNECTOR, RP-034-PHASE2-INDEX, RP-034-PHASE5-AFRICAN-LANGUAGE-ROUTING, RP-029-C, RP-033-GATE1, RP-033-GATE2, `auth-coordinator.js` (all composed, none modified) |
| RP-034-PHASE6-NO-REAL-ENCRYPTION (`core/connectivity/crypto.js` is a real, existing ES module whose own header discloses "Placeholder implementation until production crypto is integrated" — no real, verified encryption primitive exists anywhere in this repository; Phase 6's `checkEncryptionAvailable()` reports this honestly, and `canTransfer()` refuses to let PRIVATE/LOCAL_ONLY-tier data cross the real RP-033 transport as a result) | 🟡 Composed | High | Future Builder (only when real production crypto is actually integrated) | RP-034-PHASE6-PRIVACY-IDENTITY, `core/connectivity/crypto.js` |
| RP-034-PHASE6-NO-REAL-DELETION (no real, verified cascading-deletion mechanism exists across this repository's composed real stores (RP-029/030/034) — Phase 6's `executeWithdrawal()` always honestly reports CAPABILITY_UNAVAILABLE rather than claiming "deleted everywhere"; a real WITHDRAW_REQUESTED intent is recorded and preserved) | 🟡 Composed | Medium | Future Builder | RP-034-PHASE6-PRIVACY-IDENTITY |
| RP-034-PHASE7-OFFLINE-INTEGRATION (Offline Sync & Reconciliation Engine — real sync operation model (10 types), real local-first behavior, real idempotency/payload-hash verification, real versioning/conflict detection with deterministic resolution (sensitive fields always force manual review), real quarantine/African-language-routing preservation across sync, real RP-033 Gate 2 transport composition with real privacy re-evaluation at transmission time, real multi-device independent status reporting, no fabricated SYNCED/global-sync state) | 🟢 Delivered (77/77 new tests + 59 pre-existing suites passing; `CozyOS-main-RP-034-Phase7.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | Delivery session | RP-034-PHASE2-INDEX, RP-034-PHASE5-AFRICAN-LANGUAGE-ROUTING, RP-034-PHASE6-PRIVACY-IDENTITY, RP-033-GATE1, RP-033-GATE2-TRANSPORT, RP-029-C (all composed, none modified) |
| RP-034-PHASE7-COMPAREVERSIONS-FIX (Phase 7's own `compareVersions()` initially short-circuited on numeric version equality before checking real base-divergence, misreporting the spec's own explicit same-version-conflict scenario — two devices independently advancing from the same base to the same resulting version number — as UNCHANGED instead of CONFLICT) | 🟢 Fixed (base-divergence now checked before numeric equality; caught by this session's own smoke test before the test suite was written, locked in by a dedicated "same-version conflict" test) | Low | RP-034 Phase 7 session | Bug was entirely within the new Phase 7 file itself — no existing file was affected |
| RP-034-PHASE8-FINAL-INTEGRATION (Final Integration, End-to-End Certification & Release — the final RP-034 phase; a thin coordinator composing the real, complete Phase 1-7 + RP-033 chain, no new engine; real `getIntegrationStatus()`/`getCapabilityMatrix()` never upgrading a real PARTIAL/CAPABILITY_UNAVAILABLE status; a real 14-step `runCertificationScenario()` proving the actual end-to-end composition works, never fabricating SYNCED/GLOBAL_SYNCED/ALL_DEVICES_SYNCED/REMOTE_DELETED/CLOUD_BACKUP_COMPLETE anywhere; real `verifyProvenanceChain()`/`verifyIdentitySeparation()`) | 🟢 Delivered — **RP-034 FINAL CERTIFIED, all 8 phases** (86/86 new tests + 60 pre-existing suites passing; `CozyOS-main-RP-034-Phase8.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered — RP-034 milestone complete) | Delivery session | RP-034-PHASE1-CONNECTOR through RP-034-PHASE7-OFFLINE-INTEGRATION, RP-033-GATE1, RP-033-GATE2-TRANSPORT (all composed, none modified) |
| RP-034-PHASE8-ANALYSIS-FIELD-GAP (a genuine, honest integration finding surfaced by Phase 8's own certification process, not hidden: RP-030/RP-034 Phase 2's own `record.analysis` sub-object, set once at creation, is never updated by RP-034 Phase 4's separate, real job store — no function in the delivered Phase 2-4 chain links them back together; Phase 4's own `getJob()` remains the authoritative, correct source for real analysis results) | 🟡 Composed (disclosed limitation, not a regression — each phase's own delivered test suite already covers its own real behavior correctly) | Low | Future Builder (only if a caller genuinely needs Phase 2's own `record.analysis` field to reflect Phase 4 job completion) | RP-034-PHASE2-INDEX, RP-034-PHASE4-PIPELINE, RP-034-PHASE8-FINAL-INTEGRATION |
| RP-035-PHASE5-MEDIA-INTELLIGENCE-DISCOVERY (Living Media Intelligence Discovery — real `cozy-media-intelligence.js` composing Phase 2/3/4, RP-030, RP-035 Phase 1, `ServiceRegistry`, `IdentityEngine`, `ProviderManager` only; testimony discovery, provider-neutral person-appearance search with confirmed/possible separation, timestamp navigation, evidence-aware search, offline-first availability, real disclosed-deterministic `answerMediaQuestion()` for CozyAI — never claims semantic/LLM understanding; first dashboard UI for the media-intelligence line with a real, live-verified Playwright browser test) | 🟢 Delivered (50/50 Node tests + 9/9 browser tests passing, `BROWSER_TEST = PASS`; full regression 1895/16 pre-existing-unrelated; `CozyOS-main-RP-035-Phase5.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | RP-035 Phase 5 session | RP-035 Phases 1-4, RP-030, RP-035 Phase 1 country metadata, `ServiceRegistry`, `IdentityEngine` (all composed, only `identity-engine.js` intentionally extended, never re-implemented) |
| RP-035-PHASE5-CORE-APP-TIER (a genuine architectural gap surfaced by this session's own pre-implementation feasibility check, not hidden: `identity-engine.js` had no "visible without per-user assignment" application tier before this session — every non-admin dashboard was built purely from `assignApplication()`. A real `BUILT_IN` tier was added — `registerCoreApplication()`/`unregisterCoreApplication()`/`isCoreApplication()`/`listCoreApplications()`, plus a separate `coreApplications` field on `getDashboardConfig()` — deliberately kept apart from `assignedApplications` so visibility and authorization remain distinct concepts, per explicit instruction) | 🟢 Delivered (dedicated tests confirm visibility/authorization stay separate, the global disable toggle and active-account check still apply to a core app, and no existing per-user-assignment behavior changed) | — (closed, scope as delivered) | RP-035 Phase 5 session | `identity-engine.js` (intentionally extended, additive only) |
| RP-035-PHASE5-COZYAI-INTEGRATION-SCOPE (CozyAI integration for media intelligence is real but intentionally narrow: `answerMediaQuestion()` performs disclosed, deterministic keyword matching against Phase 2's real `RESEARCH_TYPES` and RP-030's real language names/codes, and a lightweight `ProviderManager` health/metadata registration mirrors `cozy-ai.js`'s own established pattern — `CognitiveCoordinator` itself was never rewired, and no semantic/LLM-quality question answering is implemented or claimed; `getCapabilityStatus().cozyAIIntegration` honestly reports `REGISTERED_METADATA_ONLY_NO_SEMANTIC_NLU`) | 🟡 Composed (disclosed scope boundary, not a bug) | Low | Future Builder (only if deeper CognitiveCoordinator-level semantic integration is judged worth the added risk) | RP-035-PHASE5-MEDIA-INTELLIGENCE-DISCOVERY, `cozy-ai.js`, `ProviderManager` |
| RP-035-SECTION13-LIVE-CONNECTIVITY-APP (Live/Connectivity Application — real `cozy-live-connectivity-app.js` composing RP-033 Gate 1 `detectCapabilities()`/`createConnectivitySession()`, Gate 2 `queue.list()`/`createPairingSession()`/`attemptBluetoothPairing()`/`getGateStatus()`, `CozyConnect.getDevices()`, `ServiceRegistry`, `IdentityEngine` only; first dashboard UI + real live-verified browser test for the connectivity line; native Wi-Fi Direct/hotspot always honestly `REQUIRES_NATIVE_COMPANION`, Bluetooth GATT honestly `CAPABILITY_UNAVAILABLE`; no fabricated CONNECTED/SYNCED/bandwidth/peer values anywhere) | 🟢 Delivered (32/32 Node tests + 8/8 browser tests passing, `BROWSER_TEST = PASS`; full regression 1935/16 pre-existing-unrelated; `CozyOS-main-RP-035-Section13-LiveConnectivity.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | RP-035 Section 13 session | RP-033 Gate 1/2, `CozyConnect`, `ServiceRegistry`, `IdentityEngine` (all composed, none modified) |
| RP-035-SECTION13-VISIBILITY-DECISION (a genuine, explicit architectural decision, not a default: Live/Connectivity is registered through `ServiceRegistry` but was deliberately NOT granted `BUILT_IN` core-application status, unlike Media Intelligence in Phase 5 — the Section 13 specification gave no instruction to make it core-visible, so it remains admin-assignable via the existing `assignApplication()` path; locked in by a dedicated negative test) | 🟢 Delivered (verified) | — (closed, scope as delivered) | RP-035 Section 13 session | RP-035-PHASE5-CORE-APP-TIER, `identity-engine.js` (read-only, not modified this session) |
| RP-035-SECTION14-LIVE-CAMERA-CAPTURE-APP (Live Camera Capture Application — real `cozy-live-camera-capture-app.js` composing `LiveVideoCapture` (real preview/photo/recording, real MediaRecorder codec negotiation, real switchCamera) and `CozyLivingConnectivity` (RP-033 Gate 1 camera/microphone permission state) only; first dashboard UI + real live-verified browser test for the camera-capture line using Chromium's real `--use-fake-device-for-media-stream` flag; capture/clarity boundary enforced at the source level — `clarityProcessed:false`/`syncState:LOCAL_ONLY` on every result, every enhancement capability permanently `CAPABILITY_UNAVAILABLE`, no such logic exists anywhere in the file) | 🟢 Delivered (37/37 Node tests + 13/13 browser tests passing, `BROWSER_TEST = PASS`; full regression 1985/16 pre-existing-unrelated; `CozyOS-main-RP-035-Section14-LiveCameraCapture.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | RP-035 Section 14 session | `LiveVideoCapture`, `CozyLivingConnectivity` (RP-033 Gate 1), `ServiceRegistry`, `IdentityEngine` (all composed, none modified) |
| RP-035-SECTION14-CAMERAENGINE-AUDIOMANAGER-MISMATCH-CONFIRMED (re-verification, not a new finding: `live-capture-engine.js`'s claim to compose "the real, existing CameraEngine/AudioManager" was independently re-confirmed false during Section 14 by loading `camera-manager.js`/`audio-manager.js` directly — both are real ES modules (`export default`) that never register `window.CozyOS.CameraEngine`/`AudioManager`; Section 14 deliberately composed `LiveVideoCapture` instead, which does not depend on either) | 🟡 Composed (disclosed limitation, unrepaired per explicit instruction) | Medium | Future Builder (only if `live-capture-engine.js`'s provider-registration path is genuinely needed) | RP-035-SECTION14-LIVE-CAMERA-CAPTURE-APP, `camera-manager.js`, `audio-manager.js`, `live-capture-engine.js` |
| RP-035-SECTION15-CAMERA-CLARITY-ENGINE (Camera Clarity Engine — real, measurement-verified staged pipeline: TONE_MAPPING/BASIC_DENOISE/SHARPEN/LOCAL_CONTRAST genuinely implemented and pixel-verified; SUPER_RESOLUTION/AI_DENOISE/DEHAZE/MULTI_FRAME_FUSION/OCR/true-HDR/NPU honestly CAPABILITY_UNAVAILABLE, none fabricated; Quality Guard genuinely computes ACCEPT/REDUCE/REJECT from real measured sharpness/clipping deltas; original never mutated, verified; first dashboard UI + real live-verified browser test for the clarity line consuming Section 14's real captured photo end-to-end) | 🟢 Delivered (36/36 Node tests + 10/10 browser tests passing, `BROWSER_TEST = PASS`; full regression 2031/16 pre-existing-unrelated; `CozyOS-main-RP-035-Section15-CameraClarity.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | RP-035 Section 15 session | `cozy-live-camera-capture-app.js` (read-only, output contract consumed not modified) |
| RP-035-SECTION15-VIDEO-CLARITY-UNIMPLEMENTED (real, disclosed scope boundary, not a defect: Section 14's `stopRecording()` returns a real `Blob`, not a `dataUrl`; real per-frame video clarity processing — frame extraction, per-frame pipeline, re-encoding — is a substantially larger implementation not attempted in Section 15; `enhance()` honestly reports `CAPABILITY_UNAVAILABLE` for any non-photo input rather than silently ignoring it or fabricating processing) | 🟡 Composed (disclosed limitation, documented per instruction not to fabricate) | Medium | Future Builder (a dedicated video-clarity milestone, if ever prioritized) | RP-035-SECTION15-CAMERA-CLARITY-ENGINE, `cozy-live-camera-capture-app.js` |
| RP-035-SECTION14-BROWSER-TEST-TIMING-FLAKE (real, one-time finding: `cozy-live-camera-capture-dashboard-browser.test.js`'s pause/resume assertions used a 150ms wait that flaked once under full-repository-regression CPU contention on an untouched, byte-identical copy of certified Section 14 code — reproduced via isolated re-runs (13/13 x3) before concluding it was environmental, not caused by any code change) | 🟢 Fixed (widened 150ms→300ms during RP-035 Section 15's mandatory gate check; 5/5 clean re-runs after the fix, full regression re-confirmed 1985/16 unchanged before Section 15 implementation began) | Low | RP-035 Section 15 session | RP-035-SECTION14-LIVE-CAMERA-CAPTURE-APP |
| COZYAI-PROJECT-KNOWLEDGE-INTEGRATION (real, narrow `FounderStory.getPublicStory(topicTag)` read path — no viewerId, public+published only, never delegates to viewer-based authorization; five new `cozy-knowledge-registry.js` fact-getters sharing one implementation; five new conversational intents composing that path only; all three privacy combinations verified; no story published this pass — every new intent honestly NOT_FOUND, ready to become VERIFIED the moment the Founder explicitly publishes real content via the existing, unmodified `setVisibility()`/`setStatus()` API) | 🟢 Delivered (48/48 new tests passing; full regression 2079/16 pre-existing-unrelated; `CozyOS-main-RP-035-CozyAI-KnowledgeIntegration.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | CozyAI Project Knowledge session | `founder-story-engine.js`, `cozy-knowledge-registry.js`, `rule-based-conversational-provider.js`, `cozy-language-templates.js` (all composed/extended, no existing behavior changed) |
| COZYAI-FOUNDER-INTENT-ORDERING-BUG (real, found during this milestone's own testing: the pre-existing bare `\bfounder\b` regex in `rule-based-conversational-provider.js`'s `founder` intent matched "why did the founder create CozyOS" before the new, more specific `project-origin` intent got a chance — a genuine intent-priority defect, not a test-authoring mistake) | 🟢 Fixed (reordered the five new project-knowledge intents ahead of `founder`, matching the file's own documented "most specific pattern first" discipline; verified via the permanent regression set) | Low | CozyAI Project Knowledge session | COZYAI-PROJECT-KNOWLEDGE-INTEGRATION |
| COZYAI-HOW-CAN-I-REGISTER-GAP (real, pre-existing gap surfaced by this milestone's required permanent regression set: "How can I register?"/"How can I create an account?" classified as `unsupported` because the existing `how-to-register` pattern only matched "how do I register"/"how to register", not the "how CAN I" phrasing) | 🟢 Fixed (broadened the existing regex to include `can\s+i`; same intent, same existing VERIFIED evidence path — only trigger-phrase recognition was extended) | Low | CozyAI Project Knowledge session | COZYAI-PROJECT-KNOWLEDGE-INTEGRATION |
| COZYAI-DUAL-LANGUAGE-REGISTRIES-DISCLOSED (architectural finding, not a defect: `core/modules/intelligence/language/cozy-language-registry.js` (the conversational provider's own language system — 5 AVAILABLE, 6 NOT_READY, 11 total) is a separate, smaller registry from RP-030's `cozy-language-pack-registry.js` (13-language media-routing registry used throughout RP-035). This milestone's multilingual verification was run against the real population of the conversational registry rather than assuming 13-language coverage already existed there) | 🟡 Composed (disclosed, not merged — merging the two registries, if ever wanted, is a separate future decision) | Low | Future Builder (only if unifying the two language registries is explicitly prioritized) | COZYAI-PROJECT-KNOWLEDGE-INTEGRATION, RP-030 `cozy-language-pack-registry.js` |
| RP-035-SECTION16-LIVE-SESSION (Live Broadcast & Living Live Surface — real orchestration-only `cozy-live-session.js` composing LiveVideoCapture (Section 14), CozyCameraClarityEngine (Section 15), CozyConnectivityTransport (Section 13, real `hostInvite()` via `PairingSession`), IdentityEngine, ServiceRegistry only; real state machine with sessionId preserved across minimize/expand/fullscreen/drag/resize/rotate/navigate, only explicit Stop/X tears down real resources; comments/live-text offline-first, never fabricate SENT; `cozy-advertising-policy.js` single decision point, ChurchOS always ADS_DISABLED, no cross-app leakage, fully separate from transport; first dashboard UI + real live-verified browser test for the live-session line) | 🟢 Delivered (52/52 Node tests + 15/15 browser tests passing, `BROWSER_TEST = PASS`, stable across 3 repeated runs; full regression 2146/16 pre-existing-unrelated; `CozyOS-main-RP-035-Section16-LiveBroadcastLivingLiveSurface.zip`, `unzip -t` clean, SHA-256 in delivery message) | — (closed, scope as delivered) | RP-035 Section 16 session | `LiveVideoCapture`, `CozyCameraClarityEngine`, `CozyConnectivityTransport`, `LiveHotspotEngine`, `IdentityEngine`, `ServiceRegistry` (all composed, none modified) |
| RP-035-SECTION16-BROADCAST-SCOPE-BOUNDARY (real, disclosed, permanent limitation — no SFU/CDN/one-to-many broadcast infrastructure exists anywhere in this repository; confirmed both by a repository-wide Rule 29 ownership scan and by the repository's own prior, independent milestone disclosures, e.g. `multi-branch-coordinator.js`'s explicit "does NOT implement broadcast/SFU/CDN — deferred to a future, separately-scoped milestone." `broadcastAvailable`/`sfuAvailable`/`cdnAvailable`/`unlimitedViewersAvailable`/`globalViewerCountAvailable` are permanently `CAPABILITY_UNAVAILABLE` in `cozy-live-session.js`) | 🟡 Composed (disclosed, correctly scoped out — not attempted, not fabricated) | Medium | Future Builder (a dedicated, separately-scoped SFU/CDN milestone, if ever prioritized) | RP-035-SECTION16-LIVE-SESSION, `multi-branch-coordinator.js`, `living-worship-player.js` |
| RP-035-SECTION16-WEBRTC-SANDBOX-LIMITATION-CONFIRMED (re-verification, not a new finding: `core/connectivity/test/browser-e2e-gate2.js` was re-run live before Section 16 implementation began — 3/9 passed, 6/9 genuine real-WebRTC-negotiation failures, TIMEOUT/NEGOTIATION_FAILED, due to no real network interfaces for ICE gathering in this sandbox. `cozy-live-session.js`'s `getCapabilityStatus()` honestly reports `peerTransportAvailable: "AVAILABLE_CODE_EXISTS"` distinctly from `peerTransportVerifiedInEnvironment: "NOT_VERIFIED_IN_THIS_ENVIRONMENT"` as a direct result) | 🟡 Composed (disclosed environmental limitation, not a code defect) | Low | N/A — environmental, not repository-owned | RP-035-SECTION16-LIVE-SESSION, `core/connectivity/test/browser-e2e-gate2.js` |
