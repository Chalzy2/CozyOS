# B4.1 — Group-B Changed-File Reconciliation

**Blocker A (B3-RUNTIME-001): CLOSED — not revisited.**
Confirmed prior finding stands: `docs/builder/knowledge/B3-POSTGRES-16OF16-RUNTIME.txt`,
SHA-256 `271a88b436d9b7088466ab401f59abeb5dcf48443d5a42dc0c236cdd4efa27f7`,
literal result `tests 16 / pass 16 / fail 0 / cancelled 0 / skipped 0`.

**This report addresses only the six Group-B files.** No git history exists
in this checkpoint (no `.git/`), so evidence below comes from: the files'
own header comments, cross-referenced against every implementation
report, checkpoint doc, and registry in the archive that names each file,
plus direct content verification where a report makes a checkable claim.
No timestamp is used as a standalone justification — where a timestamp is
mentioned it's contextual only, per your instruction.

---

### FILE: `index.html`

- **Actual change identified:** two `<link>` tags added
  (`core/ui/cozy-tokens.css`, plus the pre-existing animation/launch-sequence
  links reordered above a new comment block) so the page loads the theme
  token stylesheet.
- **Reason/purpose:** fixes a documented, specific defect — the page never
  linked any stylesheet defining the `--cozy-*` theme tokens, so every
  theme was rejected.
- **Evidence source:** `docs/history/M387.5.md`, **Finding 7** —
  "`index.html` never links the theme token stylesheets," described as
  unfixed and "flagged for next pass" as of that report. The file's own
  comment cites "M387.5 Finding 7" verbatim and the fix implemented
  (`<link rel="stylesheet" href="core/ui/cozy-tokens.css">`) is exactly the
  fix the finding calls for. I confirmed the link tag is genuinely present
  in the current file, not just claimed in the comment.
- **Relationship to B1–B3:** none — this is a front-end theming fix,
  unrelated to the PostgreSQL migration.
- **Evidence confidence: HIGH.** Independent report describes the exact
  defect; the file's fix matches it precisely; content was verified
  directly, not just the comment taken at face value.
- **Unresolved? NO.**

---

# B4.1 — Group-B Changed-File Reconciliation (Round 2: Historical Provenance)

**Blocker A (B3-RUNTIME-001): CLOSED — not revisited.**
`docs/builder/knowledge/B3-POSTGRES-16OF16-RUNTIME.txt`,
SHA-256 `271a88b436d9b7088466ab401f59abeb5dcf48443d5a42dc0c236cdd4efa27f7`,
literal result `tests 16 / pass 16 / fail 0 / cancelled 0 / skipped 0`.

**Methodology for this round:** every `*-SHA256-MANIFEST.txt`,
`CHANGED-FILE-HASHES-*.txt`, `MANIFEST-HASHES*.txt`, and named
milestone/audit report in the checkpoint was searched for each filename.
Every distinct historical hash for each file was extracted and placed in
order (where order is independently evidenced — by explicit "unchanged
since," "confirmed unchanged," or "modified in this pass" statements in
the reports themselves, not by timestamp). Current file content was
hashed and compared against every historical value found. Round-1
findings for `index.html` (RESOLVED) stand unchanged and are not repeated
here.

---

### FILE: `login.html`

**Historical hash chain found (chronological, per explicit report statements):**

| Hash (short) | Where recorded | What the record says |
|---|---|---|
| `908b92f1...` | `dashboard-baseline-reference/FULL-TREE-HASHES-CHECKPOINT-A.txt`, `MANIFEST-HASHES.txt`, `MANIFEST-HASHES-PATCH-3/4/5-AUDIT.txt`, `PROMPT4-MID-A-FULL-TREE-HASHES.txt`, `MANIFEST-CHECKPOINT-G.txt` | earliest recorded baseline |
| `a38d05e5...` | `CHANGED-FILE-HASHES-PROMPT6-MID-2.txt` | recorded as a changed-file (file was edited) |
| `ea08b484e9...` | `CHANGED-FILE-HASHES-PROMPT6-MID-3.txt`, `CHANGED-FILE-HASHES-PROMPT7-MID-1.txt`, `CHANGED-FILE-HASHES-PROMPT7-MID-2.txt` | recorded as changed again, three consecutive passes |
| `aca531e1...` | `CP6.4`, `CP6.6`, `CP6.7`, `CP6.8` manifests | stable across 4 checkpoints |
| `11ab4146...` | `CP7` through `CP16B` manifests (10 checkpoints) | stable across the entire "CP" checkpoint lineage — **CP7 explicitly labels this "confirmed unchanged (hash-verified against pre-routing-work values)"** |
| `54c1c2e4...` | `PHASE10C3A-CHECKPOINT`, `PHASE10C3B3`, `PHASE10C3B42` manifests, `CHANGED-FILE-HASHES-PROMPT9A-MID-1.txt` | a separate branch lineage (see `core/docs/MIGRATION_LOG.md`'s "Branch A/Branch B" merge) |
| **`7c8d27d4...`** | **current file only — appears in NO manifest anywhere in the checkpoint** | — |

Separately, `MODIFIED_FILES_M366.2.txt` documents a real, described edit to
`login.html` grouped under "Launch timing / glow" (alongside
`launch-sequence.css/js`, `startup-orchestrator.js`) — this is a genuine
historical edit event, though the list categorizes rather than diffs
`login.html` specifically. This M366.2 edit is not directly tied to any
of the hash values above by an explicit before/after pair in that report.

- **Actual change identified (current transition):** cannot be isolated —
  current hash matches none of the 6 distinct historical values recorded
  across the entire checkpoint history.
- **Reason/purpose:** the file's own comment claims "M387.5-repair" (a
  favicon fix). As found in Round 1, this label appears nowhere outside
  the file itself, and its factual claim ("reused verbatim" from
  `dashboard.html`) is false — `dashboard.html` has no favicon links;
  the actual matching pattern is in `admin-workspace.html` with a
  different query string.
- **Evidence source:** historical hash chain above; `MODIFIED_FILES_M366.2.txt`
  (real but unrelated prior edit); the file's own comment (uncorroborated,
  and partly contradicted, for the *current* edit).
- **Relationship to B1–B3:** none.
- **Evidence confidence: LOW** for the current transition specifically.
  This file has a long, real history of legitimate edits (M366.2, several
  PROMPT-MID passes) — it is clearly not an anomalous file in general —
  but the specific content now present has no matching historical record
  anywhere, and the one internal explanation offered for it doesn't
  survive a fact-check.
- **Unresolved? YES — UNRESOLVED, NO HISTORICAL CHANGE RECORD FOUND for the current transition.**

---

### FILE: `chalzydashboard.html`

**Historical hash chain found:**

| Hash (short) | Where recorded |
|---|---|
| `35ab7e7087...` | **Every** manifest from `CP6.4` through `CP16B` (13 checkpoints spanning the file's entire recorded history) — completely unchanged for the checkpoint's whole documented lifetime |
| **`e88ac86d...`** | current file only — in no manifest |

`docs/checkpoints/CP7-ADMIN-USER-DASHBOARD-ROUTING-CHECKPOINT.md` lists
`chalzydashboard.html` (MD5 `16cd9f8c...`) explicitly under **"Files
confirmed unchanged (hash-verified against pre-routing-work values)"** —
i.e., CP7 is not the source of any edit to this file; it only verified
the file wasn't touched by CP7's own routing work.

- **Actual change identified:** cannot be isolated. Thirteen consecutive
  checkpoints record the identical hash; the current file matches none
  of them, and no manifest or report after `CP16B` was found.
- **Reason/purpose:** unknown for the current content. CP7 establishes
  what the file is *for* (admin routing gate) but explicitly disclaims
  changing it.
- **Evidence source:** the 13-checkpoint hash chain; CP7's explicit
  "unchanged" statement.
- **Relationship to B1–B3:** none.
- **Evidence confidence: NONE for the current transition.** The historical
  record is unusually clean here (one stable value for the file's entire
  documented life) which makes the undocumented final change more
  conspicuous, not less.
- **Unresolved? YES — UNRESOLVED, NO HISTORICAL CHANGE RECORD FOUND.**

---

### FILE: `core/shell/cozy-workspace.js`

**Historical hash chain found:**

| Hash (short) | Where recorded |
|---|---|
| `1c96c31c...` | **Every** manifest found referencing this file: `dashboard-baseline-reference/FULL-TREE-HASHES-CHECKPOINT-A.txt` and `DASHBOARD-SEAM-HASHES.txt` (earliest), `MANIFEST-HASHES.txt`, `MANIFEST-HASHES-PATCH-3/4/5-AUDIT.txt`, `PROMPT4-MID-A-FULL-TREE-HASHES.txt`, `MANIFEST-CHECKPOINT-G.txt`, every `CP6.x` through `CP16B` manifest, and every `PHASE10C3A/B3/B42` manifest — one single value across the file's **entire** recorded history on both branch lineages |
| **`e0f5c56d...`** | current file only — in no manifest |

Searched specifically for "Service Registry," "registerCoordinator,"
"registerApplication," and "workspace shell" across every `.md`/`.js` in
the checkpoint: no report, checkpoint doc, or other source file besides
`cozy-workspace.js` itself references the Service Registry mechanism its
own header describes for v3.1.1.

- **Actual change identified:** cannot be isolated or corroborated at all.
- **Reason/purpose:** only the file's own header comment (v3.1.0/v3.1.1
  self-changelog) offers one — zero independent sources.
- **Evidence source:** none beyond the file itself.
- **Relationship to B1–B3:** none.
- **Evidence confidence: NONE.** This is the weakest-evidenced file of
  the six: a single hash held for the entire project history, then one
  change with literally no corroborating source anywhere in the archive.
- **Unresolved? YES — UNRESOLVED, NO HISTORICAL CHANGE RECORD FOUND.**

---

### FILE: `Firebase/firebase-auth.js`

**Historical hash chain found:**

| Hash (short) | Where recorded |
|---|---|
| `8e064491...` | **Every** manifest referencing this file across the entire checkpoint history — same pattern as the two files above: one value, unbroken, from the earliest baseline through `CP16B` and all `PHASE10C3` variants |
| **`79280138...`** | current file only — in no manifest |

`MODIFIED_FILES_M366.2.txt` explicitly states: *"Byte-identical (confirmed
via sha256, spot-checked): Firebase/firebase-auth.js, core/modules/identity/auth-coordinator.js..."*
— i.e., an actual sha256 spot-check was performed during M366.2 and
confirmed this file untouched. `FINAL_MODIFIED_FILES_M366.2.txt`
reconfirms it untouched in a second, later audit pass.
`IMPLEMENTATION-REPORT-PROMPT10B-MID-2.md`'s description of the file
(Milestone 146/220, Firebase Auth SDK wrapper, no Google sign-in exposed)
matches this long-stable version, not the new one — it predates the
undocumented change.

- **Actual change identified:** cannot be isolated. The file was
  confirmed byte-identical by name, via sha256, at least twice, then
  changed to a value that appears nowhere else in the checkpoint.
- **Reason/purpose:** not documented anywhere.
- **Evidence source:** hash chain above; two independent M366.2 sha256
  spot-check confirmations of the prior stable state.
- **Relationship to B1–B3:** none.
- **Evidence confidence: NONE for the current transition** (though the
  file's prior stability is unusually well-confirmed — twice, via actual
  sha256 checks, not just presence in a bulk manifest).
- **Unresolved? YES — UNRESOLVED, NO HISTORICAL CHANGE RECORD FOUND.**

---

### FILE: `core/modules/identity/auth-coordinator.js`

**Historical hash chain found — this file has the most complex documented lineage of the six:**

| Hash (short) | Where recorded | What the record says |
|---|---|---|
| `4dbc09c8...` | `dashboard-baseline-reference/FULL-TREE-HASHES-CHECKPOINT-A.txt`, `MANIFEST-HASHES.txt`, `MANIFEST-HASHES-PATCH-3/4/5-AUDIT.txt`, `PROMPT4-MID-A-FULL-TREE-HASHES.txt`, `MANIFEST-CHECKPOINT-G.txt` | earliest baseline; **`MODIFIED_FILES_M366.2.txt` and `FINAL_MODIFIED_FILES_M366.2.txt` both explicitly sha256-confirm this file byte-identical/untouched** through the M366.2 implementation pass and its later audit pass |
| `2ffd74a4...` | `CP7` through `CP16B` manifests (10 checkpoints) | **CP7 explicitly labels this "confirmed unchanged (hash-verified against pre-routing-work values)"** — meaning the transition from `4dbc09c8` to `2ffd74a4` happened *before* CP7, in a gap this checkpoint's evidence doesn't cover (after the M366.2 audit, before CP7) |
| `856b8ce8...` | `PHASE10C3A-CHECKPOINT`, `PHASE10C3B3`, `PHASE10C3B42` manifests, `CHANGED-FILE-HASHES-PROMPT9A-MID-1.txt` | a separate branch value — `core/docs/MIGRATION_LOG.md` documents that a Branch A/Branch B merge decision assigned `auth-coordinator.js` to "Branch A" as canonical, which is consistent with two divergent branch values existing, though the log doesn't give either branch's literal hash |
| **`1a082237...`** | current file only — in no manifest | — |

- **Actual change identified:** cannot be isolated for the final
  transition. Two earlier transitions in this file's history *are*
  evidenced (baseline→CP7 value, confirmed via M366.2's explicit
  byte-identical checks ruling that gap in; and the Branch A/B divergence
  via MIGRATION_LOG.md) — but the final step, from the long-stable `2ffd74a4`
  (held across 10 checkpoints) to the current `1a082237`, has no record.
- **Reason/purpose:** unknown for the current content.
- **Evidence source:** hash chain above; `core/docs/MIGRATION_LOG.md`;
  `MODIFIED_FILES_M366.2.txt`/`FINAL_MODIFIED_FILES_M366.2.txt`; CP7's
  explicit "unchanged" statement.
- **Relationship to B1–B3:** none.
- **Evidence confidence: MEDIUM for lineage prior to the current state**
  (unusually well-documented compared to the other five files) **/ NONE
  for the current transition itself.**
- **Unresolved? YES — UNRESOLVED, NO HISTORICAL CHANGE RECORD FOUND for the final transition.**

---

## Summary

| File | Historical values found | Current hash matches any? | Unresolved |
|---|---|---|---|
| `index.html` | (Round 1: RESOLVED via M387.5 Finding 7) | — | NO |
| `login.html` | 6 distinct values across full history | NO | YES |
| `chalzydashboard.html` | 1 value, unchanged for 13 checkpoints | NO | YES |
| `core/shell/cozy-workspace.js` | 1 value, unchanged for entire history | NO | YES |
| `Firebase/firebase-auth.js` | 1 value, unchanged + 2x explicit sha256 spot-check | NO | YES |
| `core/modules/identity/auth-coordinator.js` | 3 distinct values, 2 transitions evidenced | NO | YES |

**No amount of deeper historical search changed the outcome for any of
the five files**: every one of them has a real, traceable history right
up to the last checkpoint captured in this archive (`CP16B`), and every
one of them then has exactly one final, completely undocumented change
that produced the content now sitting in the checkpoint. This is a
consistent pattern across all five, not five unrelated gaps — whatever
produced this checkpoint's final state touched all five files after the
last point anything in the archive was willing to hash-check them.

---

## Round 3: Merged-0003 Creation Event — Direct Investigation

**Target:** find the process that produced the final "CozyOS Merged 0003"
tree after `CP16B`, treating this as one event rather than five.

**Searched for, found none of:**
- `.git`, git logs, reflogs, commit history, merge records — confirmed
  (again) no `.git/` anywhere in the archive or its parent directories.
- Patch files, backup copies of prior file versions, a "CozyOS-main"
  working directory (the manifests' internal paths reference
  `CozyOS-main/...`, suggesting one may have existed at some point, but
  no such directory or archive exists in this checkpoint).
- ZIP-creation scripts, checkpoint-generation scripts, merge scripts,
  file-copy scripts, shell history. The only `.sh` files anywhere in the
  archive are `language-packs/shared/.../real_sw_to_16.sh`,
  `scripts/verify-production-routing.sh`, and
  `core/vendor/tesseract/install.sh` — none related to packaging or
  merging the repository itself.
- Cloud/Builder execution logs — none present (no `.log` files anywhere).
- A change report immediately preceding "Merged 0003." The three
  `MERGE-NOTE-*.md` files that exist (`PHASE10C2B`, `PHASE10C3A-CHECKPOINT`,
  `PROMPT8-INTO-PROMPT7`) all carry the same early timestamp as `CP16B`
  and `MIGRATION_LOG.md` (2026-08-30 14:48:19) and none mentions any of
  the five files by name — they document earlier, unrelated merges.
- Any artifact containing both the CP16B hash and the current hash for
  any of the five files, together, in one place — searched for all five
  current hash values across every `.txt`/`.md` in the checkpoint; the
  only hits are this report and the manifest I generated myself this
  session.
- A backup or duplicate copy of any of the five files' pre-change content
  anywhere in the archive (checked `dashboard-baseline-reference/`, which
  contains only hash listings, not file copies; checked for any
  similarly-named files — found `core/security/auth-coordinator.js`,
  which is a genuinely distinct, unrelated file — different `File
  Reference` header, different global namespace (`AuthorizationCoordinator`
  vs. `AuthCoordinator`), different length (346 vs. 1,599 lines) — its own
  hash `cfde0599...` matches its own unbroken historical record perfectly
  and is not a hidden copy of the identity file).

**Consequence for the requested byte-level diff:** none of the five files'
*pre-change* content exists anywhere in this checkpoint — only its hash
was ever recorded. A hash lets me prove the content changed; it cannot
reconstruct what the old content was. I can report the hash pair for each
file, but not the literal diff, without inventing content that isn't in
evidence.

| File | CP16B SHA-256 | Current SHA-256 | Exact diff available? |
|---|---|---|---|
| `login.html` | `11ab4146213c1f98d2ba2c0ce0f83bee06508dcb6392ee45cdb1810583c7f5f3` | `7c8d27d426822d19e862ab89212af6a2b27d57e977e8f5f1403fa4fdd6abe206` | No — old content not preserved anywhere in archive |
| `chalzydashboard.html` | `35ab7e7087df570bc5507de80f755d995a38684ac5e7b09d126fe6c7bcb35e0a` | `e88ac86d133157e7946964acddb97cfc231427bb77d53a2513b56000b560ba91` | No |
| `core/shell/cozy-workspace.js` | `1c96c31c46657c9685907fdc2a6c0153616b16f02892cd0abba0d8d7c83ab8be` | `e0f5c56dd25f20e280a9fcede41a19ac5ab4bcdfb0930bffb26a722031379155` | No |
| `Firebase/firebase-auth.js` | `8e06449147b6262f8f514b02f47094f58a852e259b8a5953f6192ef780e0dedb` | `79280138afb84fde3e13cabd76a980f09b90f051b7ff02dadd83c02eacf486f6` | No |
| `core/modules/identity/auth-coordinator.js` | `2ffd74a4d7263d923e0010ccd137755cf6b23ad2aa6780cb3b7f9c781bffa719` | `1a08223722343f56a883771e4af40e4c4cc600b70d07cc0e58ee7944ec31d48a` | No |

**First known appearance of current content:** for all five, the only
place the current hash appears in this entire investigation is the "as
delivered" manifest I generated myself this session, plus the running
file on disk. There is no earlier appearance anywhere in the archive.

**Process/tool that produced it:** cannot be identified. No artifact in
the checkpoint names, logs, or otherwise evidences a merge tool, script,
or manual editing session responsible for these five changes.

> **MERGED-0003-PROVENANCE-001 — FINAL POST-CP16B CHANGE EVENT NOT RECOVERABLE**
>
> The process that produced the current content of `login.html`,
> `chalzydashboard.html`, `core/shell/cozy-workspace.js`,
> `Firebase/firebase-auth.js`, and `core/modules/identity/auth-coordinator.js`
> cannot be reconstructed from anything in this checkpoint. No git
> metadata, script, log, patch, backup copy, or report — old or new —
> documents it. This is a genuine, confirmed gap, not a search failure:
> every category of evidence requested was checked and returned nothing.

## Final status (unchanged in substance, now exhaustively confirmed)

**STATUS = B4.1 BLOCKED**

1. `login.html` — UNRESOLVED — NO HISTORICAL CHANGE RECORD FOUND
2. `chalzydashboard.html` — UNRESOLVED — NO HISTORICAL CHANGE RECORD FOUND
3. `core/shell/cozy-workspace.js` — UNRESOLVED — NO HISTORICAL CHANGE RECORD FOUND
4. `Firebase/firebase-auth.js` — UNRESOLVED — NO HISTORICAL CHANGE RECORD FOUND
5. `core/modules/identity/auth-coordinator.js` — UNRESOLVED — NO HISTORICAL CHANGE RECORD FOUND

`index.html` remains the only resolved Group-B file. No further search of
this static archive will change this outcome — the evidence that would
resolve it (a merge/build log, a git history, or a pre-change backup of
any of the five files) does not exist in what's been supplied. Closing
this blocker now requires either locating that artifact outside this
checkpoint (wherever "Merged 0003" was actually assembled) or accepting
these five files as a permanently unresolved, disclosed gap rather than a
gate that further inspection of the same ZIP can clear.

