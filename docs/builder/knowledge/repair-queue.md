# Cozy Builder — Repair Queue

Per Rule 62 (`docs/builder/rules/07-repair-queue-rule.md`). Permanent,
append-only log — one row per finding, created the moment a finding is
Composed, never deleted on Close (only updated to its final state). This is
the authoritative work-item list; `HANDOFF.md`'s "Repair Queue Summary"
is a filtered view of this file (open items only, grouped by priority).

Status values use the Rule 61 taxonomy: 🟡 Composed · 🟠 Planned ·
🔵 Implementing · 🟢 Fixed · 🔴 Failed Verification · ⚪ Deferred.

| ID | Status | Priority | Owner | Depends On |
|---|---|---|---|---|
| RP-007 (Finding 1 — `developer-hub.css` doubled path) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-008 (Finding 2 — `SESSION_STATE` collision) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-009 (Finding 3 — `pluginManager.js` semver) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-010 (Finding 6 — `register()` handler mismatch, 23 call sites) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-011 (Finding 7 — `index.html` missing theme tokens) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-012 (Finding 9 — `EngineBridge` Node `fs` in browser) | 🟢 Fixed | — (closed) | M387.5 session | None |
| RP-013 (AA-004 — `AudioEngine` naming collision) | 🟢 Fixed | — (closed) | M387.5b session | None |
| RP-014 (premature `restoreSession()` auto-trigger wipes valid Remember Me pointer) | 🟢 Fixed | — (closed) | M387.5c session | None |
| RP-015 (trusted-pointer fallback always re-persists with rememberMe=true) | 🟢 Fixed | — (closed) | M387.5c session | None |
| MD-007 (no bundled machine translation engine) | 🟡 Composed | High | Future Builder (M388 Plan) | Licensing/vendor decision — Out of Scope this Compose |
| MD-008 (no voice-cloning/neural TTS capability) | 🟡 Composed | High | Future Builder (M388 Plan) | Out of Scope this Compose |
| MD-009 (no video/audio codec decode/encode capability) | 🔵 Implementing | High | M388 Engine 1 session (decode half, Closed); Engine 9 (encode half) Closed this pass — real structural mux plan delivered, real codec bytes remain open | Decode half: `core/engines/media/decode/media-decode-engine.js` implemented (honest reference provider — real container detection, no fabricated decode). Encode half (Engine 9): Phase 0–9 all complete this pass (`docs/history/M388-E9-MediaEncode-Compose.md`) — `buildEncodePlan()` produces a real, deterministic mux plan cross-referencing Engine 8's timing classification against Engine 7's playback results (`realEncode: false`, honest). 12/12 real tests pass; Engines 1–8's 166 regression tests unaffected. Real codec decode/encode bytes remain a Plan-stage decision per `AA-006`/Compose §4 — not resolved by Engine 9, same honesty boundary as the decode half. |
| MD-010 (no background/ambient audio separation) | 🔵 Implementing | Medium | M388 Engine 5 session (Phase 3 Implementation done this pass — real, deterministic diarization-turn-coverage partitioning into speech/unclassified; honest empty envelope otherwise; not yet Verified/Closed) | MD-009 |
| MD-011 (no speaker diarization capability) | 🔵 Implementing | Medium | M388 Engine 4 session (Phase 3 Implementation done this pass — real, deterministic contiguous speaker-hint turn-grouping when a hint is available; honest empty envelope otherwise; not yet Verified/Closed) | None |
| MD-012 (no automatic language-detection capability) | 🔵 Implementing | Medium | M388 Engine 2 session (Phase 3 Implementation done this pass — real Unicode-script + curated lexical-heuristic detection when text is available; honest empty envelope otherwise; not yet Verified/Closed) | None |
| DI-004 (`core/language.js:32` references `window.CozyLanguage?.LANGUAGES`, but `window.CozyLanguage` is never assigned anywhere in the repository — masked by optional chaining + hardcoded fallback, not an active defect) | Open | Low | Future Builder (unrelated to M388; discovered during Engine 2 Phase 0 repository search) | None |
| MD-013 (no streaming/real-time low-latency transcode pipeline) | 🔵 Implementing | High | M388 Engine 10 session — **Phase 3 through Phase 9 all complete, Engine 10 CLOSED.** `core/engines/media/streaming/streaming-pipeline-engine.js` (new file). Real per-stream segment latency/throughput instrumentation over `cozy-live.js`'s existing Stream/TranslationStream state; `getCapabilities().realLowLatencyTransport` honestly `false`. 21/21 real tests pass; 178/178 prior-engine tests re-run clean (199/199 total, reconfirmed at Close). The underlying transport gap (no WebRTC/streaming-transcode backend) remains genuinely unresolved — Engine 10's own scope was instrumentation only, per its Implementation Contract items 4/5. MD-013 itself remains 🔵 Implementing as a Repair Queue entry because it tracks the capability, not the engine's own now-Closed lifecycle | MD-009 |
| MD-014 (no subtitle export/rendering capability) | 🔵 Implementing | Low | M388 Engine 6 session (Phase 3 Implementation done this pass — real, deterministic cue-timeline construction + .srt export from caller-supplied real text/timing; honest skip/empty envelope otherwise; not yet Verified/Closed) | None |
| MD-015 (lip-sync capability confirmed absent repository-wide) | 🟡 Composed | Low (Out of Scope this Compose) | Future Builder (later milestone) | MD-009, MD-011 |
| AA-005 ("Living Meaning Engine" has no defined real scope) | 🟢 Fixed | — (closed) | M388 Phase 2 | None — merged into Living Translation Engine, documented decision |
| AA-006 (MD-009 registry text conflated Engine 1/9's media-file demux/mux scope with the already-reserved codec-decoding-engine.js/codec-encoding-engine.js still-image-codec file paths) | 🟢 Fixed | — (closed) | M388 Engine 1 Compose | None — Engine 1 assigned its own new file path, documented decision |
| MD-016 (no engine in the Approved 11-engine Implementation Order owns the audio-buffer → SpeechRecognitionAdapter bridging step identified in Engine 1's Compose §5) | 🟡 Composed | Medium | Future Builder (M388 Plan/Implementation) | Needs an owning engine assigned — most naturally Engine 1's own Implementation or an explicit hand-off contract on whichever engine calls STT first |
| MD-017 (no production registrant for `cozy-live.js`'s mandatory `'CozyTranslate'`/`'CozySpeech'` subsystem slots — `relaySpeechSegment()` cannot complete a call today; confirmed via repository-wide `registerSubsystem(` search) | 🟢 Fixed (`'CozyTranslate'` half only) | High → carried at High for the unresolved `'CozySpeech'` half | M388 Engine 3 session — `'CozyTranslate'` half resolved this pass (`core/engines/media/translation/translation-pipeline-engine.js`, `attachToLive()`); `'CozySpeech'` half remains unassigned | Resolved: `translation-pipeline-engine.js` registers a real `'CozyTranslate'` adapter (12/12 real tests pass). Still open: no engine has yet registered `'CozySpeech'` — `relaySpeechSegment()` still cannot complete a call in production until that half is also resolved |
| MD-018 (`relaySpeechSegment` computes `detectedLanguage` but never forwards it to the `'CozyTranslate'` adapter's `translate()` call) | 🟡 Composed | Medium | Future Builder (unassigned — not Engine 3's scope) | Resolution decided at Engine 3 Phase 2 Review: requires editing `relaySpeechSegment()` itself (hardcoded `session.primaryLanguage` argument), which Engine 3's Implementation Contract forbids touching; no exception granted; carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016` |
| MD-004 (Finding 8 — 3 missing media engine files) | ⚪ Deferred | Medium | Future Builder | Feature milestone (building real codec/background engines is out of scope for a verification pass) |
| MD-005 (`provider-browser.js` missing, discovered while closing AA-004) | ⚪ Deferred | Medium | Future Builder | Feature milestone (real `getUserMedia`-backed provider is feature-scale work; Camera has the identical gap) |
| AA-004 (`window.CozyOS.AudioEngine` naming ambiguity) | 🟢 Fixed | — (closed) | M387.5b session | None |
| DI-005 (documented repository-hashing method silently mis-splits filenames containing spaces, the real root cause of the Round 10/11/12 SHA-256 discrepancy) | Resolved | — (closed) | M388 Engine 2 session (Phase 3/4, unrelated to Engine 2's own build) | None — canonical `-print0`/`-z`/`-0` method adopted this pass |
| MD-019 (`cozy-live.js`'s `relaySpeechSegment()` has no optional subsystem hook analogous to `CozyLanguage`/`CozyKnowledge` for automatic speaker assignment) | 🟡 Composed | Medium | M388 Engine 4 session (Compose + Phase 2 Review) | **Decided at Phase 2 Review:** no exception granted to add a new hook to `cozy-live.js` — Engine 4 revised to be fully external (writes into `cozy-speech.js`'s `_speakers` registry only). Remains open/unassigned, same treatment as `MD-016`. |
| MD-020 (no existing or newly-buildable-this-pass capability can capture synthesized speech from the Web Speech API as an audio buffer/track — every existing TTS path, and the browser API itself, is playback-only) | 🔵 Implementing | High | M388 Engine 7 session — **Phase 3 (Implementation) through Phase 9 (Close) all complete this pass.** 13/13 real tests pass; 129/129 prior-engine tests re-run clean (142/142 total this pass). Orchestration layer (`generateSpeechForSegment`/`generateSpeechForSegments`) Verified. Buffer-capture question itself remains genuinely unresolved — not attempted this pass, per Compose §9/§12 item 6; Engine 7's own scope was orchestration only, not solving MD-020 | Found during Engine 7's Phase 0/Compose, confirmed unchanged at Phase 2 Review, still unresolved after Engine 7's Close. **Re-confirmed unchanged at Engine 9's own Phase 0/Compose this pass** (`docs/history/M388-E9-MediaEncode-Compose.md`) — directly blocks Engine 9's ability to claim any real encoded audio output; Engine 9's Draft Implementation Contract explicitly does not attempt to resolve it. Carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016`/`MD-018` |
| DI-006 (`docs/builder/knowledge/milestone-waiting-queue.md` named the milestone "Living Live Interpretation"; `LATEST.md`/`HANDOFF.md`/`RELEASES.md` all name it "Living Media Interpreter") | 🟢 Fixed | — (closed) | M388 Engine 4 session (Phase 0) | Corrected in the Waiting Queue this pass to match the other three files |
| MD-021 (no engine in the Approved 11-engine order produces a real audio duration or buffer — Engine 1's decode is a structural-only `isReal:false` envelope, Engine 7's speech generation is playback-only with `realAudioBuffer:false` — so no component in this repository can compute a real numeric timing offset/drift between generated speech and the original video) | 🔵 Implementing | High | M388 Engine 8 session — Phase 3/4 complete: real `crossCheckTiming()` classification implemented and verified (never fabricates a drift value; `getCapabilities().realDriftMeasurement` honestly `false`). The underlying constraint (no real drift number computable) remains genuinely unresolved — Engine 8's own scope was cross-check classification only, per its Implementation Contract item 5 | `MD-020`, `MD-009` (decode half) |
| AA-008 (four pre-existing, unrelated modules have names invoking "synchronization" — `core/modules/sync/cozy-sync.js`, `core/connectivity/sync.js`, `core/living/cozy-living-sync.js`, `core/connectivity/conflict.js` — checked for duplicate-engine risk against the new Engine 8 "Synchronization Engine". **Revised at Phase 2 Review:** original scan (`grep -ril "synchroniz\|timesync\|drift\|timing.*align"`) was incomplete — re-run independently found two more real hits: `modules/live/cozy-live.js` (`syncTimestamp()`, a session/room-level checkpoint-broadcast mechanism) and `core/network/cozy-network-orchestrator.js` (`#stampMediaSync()`, transport-layer sequence/clock stamping). Both checked directly and confirmed not duplicates — different data model and purpose from Engine 8's per-`segmentId` cue-vs-playback classification.) | 🟢 Fixed | — (closed) | M388 Engine 8 session (Compose Phase 1; scan corrected at Phase 2 Review) | None — six modules total now documented, all confirmed no collision; Engine 8 placed at a distinct path (`core/engines/media/synchronization/`) |
| MD-022 (`core/engines/media/record-export-session-manager.js` explicitly disclaims "Frame synchronization -> Scene Manager" as out of its own scope, but no "Scene Manager" module exists anywhere in the repository — confirmed via repository-wide search. Found while re-verifying Engine 8's `AA-008` naming-collision scan at Phase 2 Review) | 🟡 Composed | Low | Future Builder (tangential to Engine 8 — different concept, video-frame-level sync for local recording/export vs. Engine 8's segment-timing cross-check; Engine 8 does not depend on it) | None |
| DI-007 (`HANDOFF.md`'s Rule 72 roadmap-header block was not regenerated when Round 18's trailing section closed Engine 3/unlocked Engine 4, leaving the header stating stale Engine 3/4 status) | 🟢 Fixed | — (closed) | M388 Engine 4 session (Phase 0) | Header block corrected this pass to match the file's own trailing section |
| AA-007 (`core/engines/media/media-pipeline-manager.js` already imports an unbuilt, VISUAL `background-engine.js` — one of `MD-004`'s missing files, grouped with `image-engine.js`/`filter-engine.js`/`enhancement-engine.js` — creating a naming-collision risk with Engine 5's "Background Audio Separation Engine") | 🟢 Fixed | — (closed) | M388 Engine 5 session (Phase 1 Compose) | Engine 5 deliberately placed at `core/engines/media/audio-separation/background-audio-separation-engine.js`, a distinct path — no collision with the reserved-but-unbuilt visual `background-engine.js` |
| DI-008 (`LATEST.md`'s top summary block was found stale at the start of the Engine 10 Phase 3 round — still reading "Engine 9 Phase 2, not started" / "Engine 10 LOCKED" while `RELEASES.md` already recorded Engine 9 Closed and Engine 10 Phase 0–2 Approved. `LATEST.md`, `HANDOFF.md`, and `RELEASES.md` all referenced this finding by id, but no row for it was ever actually added to this file — a real Rule 62 process gap, found this pass) | 🟢 Fixed | — (closed) | M388 Engine 10 session (Phase 3 round: found + corrected in `LATEST.md`; this row backfilled at Phase 5, Engine 10 Close round, since it was missing) | `LATEST.md`'s top summary corrected to match `RELEASES.md` (the authoritative ledger) in the round it was found; this Repair Queue row itself was never written until now — logged honestly as its own gap rather than silently backfilled without a note |
| DI-009 (the ZIP delivered at the start of the Engine 10 Close round claimed Repository SHA-256 `1c9467750816deb4fe33b2573f63a78e80cfcb9e0995b213c160673fd44f1dba`, computed via this repository's own canonical method; independent Phase 0 re-verification against the actual extracted ZIP contents — same method, re-confirmed under explicit `LC_ALL=C` to rule out a locale/sort artifact — produced `92adfd8ef288f18c2218d311f47ce014b9cfce558b2ad6e81f781451e038b2b2` instead, a genuine mismatch, not found recorded anywhere else in `RELEASES.md`'s own history) | 🟢 Fixed | — (closed) | M388 Engine 10 session (Phase 5, Close round — Phase 0 re-verification) | Per Rule 69, the actual extracted repository content is authoritative over any claimed hash; `92adfd8e...` adopted as the verified starting-state hash for this round. Root cause not determinable from this session alone (no diff trail against whatever produced `1c946775...`); logged as a documentation-integrity finding, same treatment as `DI-005`'s prior hash-discrepancy class, rather than silently accepted or silently ignored |

| DI-010 (`core/engines/media/record-export-session-manager.js`'s docstring lists "Frame synchronization -> Scene Manager" as an unbuilt future capability; `MD-022` recorded this as "no 'Scene Manager' module exists anywhere in the repository" — direct re-verification during Engine 11's Phase 0/1 found `core/engines/scene/scene-manager.js` **does** exist, registered as `SceneEngine` in `engine-bridge-bootstrap.js`, and is independently named the real Scene owner in `media-pipeline-manager.js`'s own locked-ownership header) | 🟢 Fixed | — (closed) | M388 Engine 11 session (Phase 0/1) | `MD-022`'s substantive conclusion (no frame-sync-for-export capability exists) is unaffected and remains correct — `scene-manager.js`'s real, verified scope is production camera/audio scene switching, not frame-level sync for exported recording sessions. Only `MD-022`'s literal phrasing ("no Scene Manager module exists anywhere") was inaccurate; corrected here. Tangential to Engine 11 — this coordinator does not depend on either file. |

## Currently Open (for quick reference — full detail in each finding's own registry)

**High:**
- MD-009 — Engine 1 (decode half) and Engine 9 (encode half) both Closed. MD-009 remains open/High only for real codec decode/encode bytes, a Plan-stage decision out of scope for Engines 1/9. MD-013 (streaming, Engine 10) — **Engine 10 itself is now CLOSED (Phase 0–9 all complete).** `core/engines/media/streaming/streaming-pipeline-engine.js` (new file) composes `cozy-live.js`'s real `getStream()`/`relaySpeechSegment()`-recorded segment timestamps into real per-stream latency/throughput metrics, never fabricating a transport-level latency it didn't observe (`getCapabilities().realLowLatencyTransport` honestly `false`). 21/21 real tests pass; 178/178 prior-engine tests re-run unmodified (199/199 total, reconfirmed at Close, zero regressions). `modules/live/cozy-live.js` and `core/engines/playback/playback-engine.js` confirmed byte-identical to the pristine checkout. MD-013's own underlying transport gap (no WebRTC/streaming-transcode backend anywhere in this repository) remains genuinely unresolved — Engine 10's scope was instrumentation only, per its Implementation Contract items 4/5. **Engine 11 (Video Interpreter Coordinator) — Phase 0 (Repository
Verification) and Phase 1 (Compose) both complete this pass**
(`docs/history/M388-E11-VideoInterpreterCoordinator-Compose.md`).
Anti-duplication scan clean — no existing video-interpreter/coordinator/
orchestration capability found anywhere; `core/engines/media/
coordinator/` confirmed free. Draft 7-item Implementation Contract
recorded, not yet approved. **Phase 2 (Review/Approval) is next —
not started this pass.**
- MD-007, MD-008 — real, confirmed gaps, but **cannot be resolved within M388 at all** (Phase 2 Review found the original task explicitly lists "Licensing of translation/voice models" as Out of Scope) — not just deferred, structurally excluded from this milestone's contract. Remain High priority as a standing gap, owned by a future milestone.
- MD-017 — found during Engine 3's Phase 0, re-confirmed (fresh repository-wide `registerSubsystem(` search, not just re-read) during Engine 3's Phase 2 Review (`docs/history/M388-E3-Translation-Compose.md`). `cozy-live.js`'s `relaySpeechSegment()` requires both `'CozyTranslate'` and `'CozySpeech'` subsystems via `getSubsystemOrThrow()` (mandatory, not optional like Engine 2's `'CozyLanguage'` hook) — zero production registrants exist for either (only Engine 2's own `'CozyLanguage'` registrant exists in production). The live pipeline cannot complete a call today. **Update, Engine 3 Phase 3 (Implementation, this pass):** `core/engines/media/translation/translation-pipeline-engine.js` built and registered into `core/bridge/engine-bridge-bootstrap.js`'s `REGISTRATIONS`; its `attachToLive()` registers a real `'CozyTranslate'` adapter via `cozy-live.js`'s own public `registerSubsystem()` API, per the Final Implementation Contract exactly as written (no locked file touched). 12/12 real, executed tests pass (`core/engines/media/translation/tests/translation-pipeline-engine.test.js`), including confirming the registered adapter's `translate()` returns exactly `{ text }` and never throws. The `'CozyTranslate'` half of `MD-017` is now resolved; the `'CozySpeech'` half remains unassigned, same status as `MD-016`.
- MD-020 — found during Engine 7's Phase 0/Compose (`docs/history/M388-E7-VoiceGeneration-Compose.md`), re-confirmed unchanged at Phase 2 Review (repo-wide search for `MediaStreamAudioDestinationNode`/`createMediaStreamDestination`/`getDisplayMedia` found no audio-capture path from `speechSynthesis`). **Engine 7 itself — Phase 0 through Phase 9 all now complete, CLOSED this pass.** `core/modules/speech/generation/voice-generation-engine.js` (orchestration-only, new file) composes `window.CozyOS.VoiceManager.speak()`, falling back to `window.CozyOS.CozyTTSBrowserAdapter.speakPreview()` only if `VoiceManager` is unavailable — neither existing file modified (confirmed unchanged this pass). **13/13 real tests pass** (`core/modules/speech/generation/tests/voice-generation-engine.test.js`); all 6 prior engines' suites re-run unmodified, 129/129 pass (142/142 total this pass, zero regressions). MD-020's own underlying question — no way to capture synthesized speech as a real audio buffer/track — remains genuinely unresolved; Engine 7's scope was orchestration only, per its own Implementation Contract item 6 (must not attempt Option B/C). Still blocks Engine 9 (Media Encode)'s actual output — carried forward as its own future, narrowly-scoped fix, same treatment as `MD-016`/`MD-018`.
- MD-021 — new at Engine 8's Phase 0/Compose (`docs/history/M388-E8-Synchronization-Compose.md`). Direct confirmation that no engine in the Approved 11-engine order produces a real audio duration or buffer (Engine 1's decode: structural-only `isReal:false`; Engine 7's speech generation: playback-only, `realAudioBuffer:false` in every code path) — so no component can compute a real numeric timing offset/drift between generated speech and the original video. Not an Engine 6 or Engine 7 defect — an environment-level constraint discovered at Engine 8's Compose. **Engine 8 itself — Phase 0 through Phase 4 now complete this pass.** `core/engines/media/synchronization/synchronization-engine.js` (new file) implements the honest scope confirmed at Phase 2 Review: a real, deterministic `crossCheckTiming()` classification (`aligned`/`timing-without-playback`/`playback-without-timing`/`unresolved`) — never a fabricated drift value. **21/21 unit tests + 3/3 real integration tests pass** (against the actual `subtitle-timeline-engine.js`/`voice-generation-engine.js`, not fixtures); all 7 prior engines' suites re-run unmodified and pass (the one pre-existing `media-pipeline-manager.test.js` failure is `MD-004`/`MD-009`, confirmed identical in the pristine, unmodified checkout — not a regression). MD-021's own underlying question — no way to compute a real drift number — remains genuinely unresolved; Engine 8's scope was cross-check classification only, per its Implementation Contract item 5. Still blocks any future "true" audio-timing synchronization, same relationship `MD-020` has to Engine 9.
- MD-022 — found during Engine 8's Phase 2 Review while re-verifying `AA-008`. Informational/tangential, not this engine's scope — unaffected by Engine 8's Implementation. **Phrasing corrected this pass (`DI-010`, Engine 11 Phase 0/1): a module named "Scene Manager" does exist (`core/engines/scene/scene-manager.js`), but its real, verified scope (production camera/audio scene switching) does not include the frame-level export-synchronization capability `record-export-session-manager.js`'s docstring gestures at — MD-022's substantive conclusion stands, only its literal wording was inaccurate.** See full detail above and in `docs/history/M388-E8-Synchronization-Compose.md` / `docs/history/M388-E11-VideoInterpreterCoordinator-Compose.md`.

**Medium:**
- MD-004 — low operational risk (already fails closed), deferred pending a dedicated feature-scope pass.
- MD-005 — same treatment as MD-004; discovered only after AA-004's fix let the relevant code path execute for the first time.
- MD-010 — Engine 5's Phase 0–2 all complete (`docs/history/M388-E5-BackgroundAudioSeparation-Compose.md`) — Approved (not Revised) at Phase 2 Review. **Phase 3 (Implementation) done this pass**: `core/engines/media/audio-separation/background-audio-separation-engine.js` + `provider-turn-coverage.js` (new files only, deliberately at a path distinct from the unrelated, still-unbuilt visual `background-engine.js` — see `AA-007`). Real, deterministic partition of segments into `speech` (covered by a real Engine 4 diarization turn) vs. `unclassified` (never `background` — no positive signal supports that inference) when diarization turn data is supplied; an honest `isReal:false`/`method:'no-analyzable-signal'` empty envelope otherwise. **18/18 real tests pass** (`core/engines/media/audio-separation/tests/background-audio-separation-engine.test.js`), including a real end-to-end composition test against Engine 4's own `diarize()` output (no mocking). Now 🔵 Implementing (Verification/Close — Phase 4–9 — not yet done this pass).
- MD-011 — Engine 4's Phase 0–2 all complete (`docs/history/M388-E4-Diarization-Compose.md`) — Approved (Revised) at Phase 2 Review. **Phase 3–9 all complete, Engine 4 CLOSED.** `core/engines/media/diarization/speaker-diarization-engine.js` + `provider-speaker-hint.js` (new files only, per the Final Implementation Contract — no locked file touched, no exception). Real, deterministic grouping of contiguous caller-supplied speaker hints into turns (`diarizeSegments()`); results are written into `cozy-speech.js`'s existing `_speakers` registry only through its already-public `registerSpeaker()`/`addActiveSpeaker()` methods (`applyToSpeechRegistry()`). With no hint on any segment, an honest `isReal:false`/`method:'no-analyzable-signal'` empty envelope is returned — no fabricated speaker count or turn boundary, since no acoustic diarization model exists in this environment (same constraint as Engine 1's `isReal:false` audio-track envelope). **23/23 real tests pass** (`core/engines/media/diarization/tests/speaker-diarization-engine.test.js`). Engine 4 itself is Closed (Phase 9); `MD-011` remains 🔵 Implementing as a Repair Queue entry because it tracks the capability, not the engine's own lifecycle — now consumed as a real upstream signal by Engine 5 (see `MD-010`, above).
- MD-019 — found during Engine 4's Compose (Phase 1), decided at Engine 4's Phase 2 Review: no exception granted to touch `cozy-live.js`; Engine 4 revised to stay fully external. Remains open/unassigned, same treatment `MD-016` got.
- MD-012 — Engine 2's Phase 0/Compose/Phase 2 Review all complete (`docs/history/M388-E2-LanguageDetection-Compose.md`). **Phase 3 (Implementation) done this pass**: `core/engines/media/language/language-detection-engine.js` + `provider-lexical.js` (new files, per the unrevised Implementation Contract). Real Unicode-script classification (Ethiopic → Amharic) and a real, deliberately-partial curated lexical-overlap heuristic (en/fr/sw/so/ha/yo/zu/lg) run only when text is actually available for a segment (explicit `hintText` or a duck-typed property on the opaque `audioRef`); otherwise an honest `isReal:false` empty envelope is returned — no fabricated acoustic detection, since no such model exists in this environment. Registered into `cozy-live.js`'s existing `CozyLanguage` subsystem slot only via `registerSubsystem()` (`attachToLive()`), never by editing `cozy-live.js` itself. **31/31 real tests pass** (`core/engines/media/language/tests/language-detection-engine.test.js`). Now 🔵 Implementing (Verification/Close — Phase 4-9 — not yet done this pass).
- MD-016 — found during Engine 1's Phase 2 Review; a real milestone-level sequencing gap (no owner yet for the audio-buffer→STT bridge), not an Engine 1 defect. Checked again during Engine 2's Phase 0, and again during Engine 3's Phase 0 — confirmed adjacent but non-blocking each time.
- MD-018 — found during Engine 3's Phase 0, resolution path decided during Engine 3's Phase 2 Review (`docs/history/M388-E3-Translation-Compose.md`). `relaySpeechSegment` computes `detectedLanguage` (via Engine 2's optional `'CozyLanguage'` hook) but never forwards it into the `'CozyTranslate'` adapter's `translate()` call — a real gap inside `cozy-live.js`'s own already-shipped pipeline logic (the `sourceLanguage` argument passed to `translate()` is hardcoded to `session.primaryLanguage`, not reachable by an external adapter). **Decided this Review:** fixing it requires editing `relaySpeechSegment()` itself, which Engine 3's Implementation Contract forbids — no exception granted; remains open and unassigned, carried forward as its own future fix, not Engine 3's scope.

**Low:**
- MD-014 — Engine 6's Phase 0–2 all complete (`docs/history/M388-E6-SubtitleTimeline-Compose.md`) — Approved (not Revised) at Phase 2 Review. **Phase 3 (Implementation) done this pass**: `core/engines/media/subtitles/subtitle-timeline-engine.js` + `provider-srt-formatter.js` (new files only — no locked file touched, confirmed via `diff -rq` against the Engine-5-closed baseline). Real, deterministic cue-timeline construction (sorting, sequential cue numbering, real overlap detection) and `.srt` export, only from caller-supplied segments that already carry real text and real millisecond timing; a segment missing either is honestly skipped and reported in `skippedSegmentIds`, never fabricated — this engine has no transcription, translation, or timing-inference capability of its own. **22/22 real tests pass** (`core/engines/media/subtitles/tests/subtitle-timeline-engine.test.js`; one test-file bug — sorting a frozen array in place — was caught and fixed during Phase 4's verify-fix-reverify loop, not an engine defect). Now 🔵 Implementing (Verification/Close — Phase 4–9 — not yet done this pass).
- MD-015 — lip-sync capability confirmed absent repository-wide, explicitly Out of Scope this Compose per the task itself. Unaffected by Engine 6.
- DI-004 — `core/language.js:32` dead-reference to an unassigned `window.CozyLanguage` global, discovered during Engine 2's Phase 0 repository-wide search. Pre-existing, unrelated to M388, not fixed this pass (out of this session's explicit no-fixes scope). Full detail: `docs/builder/knowledge/documentation-integrity-registry.md`.

**Resolved this pass:**
- DI-011 — found during Engine 11's own Phase 4 Verification (independent
  re-read, not a repeat of the prior round's claims): `LATEST.md` and
  `HANDOFF.md` each carried a second, separate "Current Engine" status
  block (further into each file, distinct from the top-of-file summary)
  that still read "11 (Phase 0–1 of 9 complete this pass)" — stale from
  Round 15, missed when Round 16's top-of-file summaries were written.
  Corrected in both files this pass to "Phase 0-3 of 9 complete this
  pass." No code affected; documentation-only.
- MD-023 — found during Engine 11's own Phase 2 Review: `translateSegment()`
  (Engine 3) requires a live `speechTranslationAdapter` instance as its
  first argument and fails closed without one — not called out explicitly
  in Engine 11's Draft Contract. Folded into the approved contract:
  the coordinator accepts it as an optional param and honestly skips
  Translation when absent, recording the skip. No engine's own code
  changed; resolved at the coordinator's call-site only.
- MD-024 — found during Engine 11's own Phase 3 Implementation: Engine 9's
  `buildEncodePlan()` hard-requires an array for `speechResults` and a
  real `{results:[...]}` object for `syncResult` — throws `TypeError` on
  `null`, so it cannot honestly run when Engine 8 (Synchronization) was
  skipped. Coordinator corrected to cascade the skip (Encode now honestly
  skipped whenever Synchronization is), matching the real 7 → 8 → 9
  dependency chain in code, not just in prose. **10/10 real Engine 11
  tests pass** (`core/engines/media/coordinator/tests/video-interpreter-coordinator.test.js`),
  including a dedicated test for this cascade.
- DI-005 — the documented repository-hashing method mis-splits the three filenames in this repository that contain spaces; this was the real, previously-unidentified root cause of the Round 10/11/12 SHA-256 discrepancy earlier rounds correctly logged as open rather than guessed at. Canonical method corrected (`-print0`/`-z`/`-0`), reproducibly confirms Round 12's own recorded hash. Full detail: `docs/builder/knowledge/documentation-integrity-registry.md`.
- DI-008 — backfilled this pass. A real finding from the Engine 10 Phase 3 round (`LATEST.md`'s stale top summary, corrected that round) was referenced by id in three files but never actually given its own Repair Queue row, a Rule 62 process gap. Row added retroactively at Engine 10's Close round; no further action needed, the underlying `LATEST.md` staleness was already fixed when found.
- DI-009 — Engine 10 Close round's own Phase 0 re-verification found the incoming ZIP's claimed Repository SHA-256 did not match the hash independently computed from its actual extracted contents (confirmed reproducible, not a locale/sort artifact). Per Rule 69, the independently-verified hash (`92adfd8e...`) was adopted as this round's authoritative starting state rather than the claimed one; Engine 10's own implementation/tests were not affected (Phase 3/4 work and its 199/199 test result predate and are independent of this hash question). Root cause of the original discrepancy not determined this pass — flagged for any future session that can trace it.

## Not Yet Composed (known future work, no Repair Queue entry yet — listed here only so it isn't lost, not a substitute for a real Compose stage)

- Interactive auth-flow verification (registration, login, logout,
  remember-me, reload, session restore, OTP, recovery codes, trusted
  device) — has not been Composed yet; no finding exists until real browser
  testing actually surfaces one.
- Mobile/Android verification — same; not yet Composed.
- `LivingDecisionEngine.recordOutcome()` — carried over from M387 as a known
  gap, not yet formally Composed as its own finding.
