# B4.1 — Final Gate Result

**Status: B4.1 BLOCKED**

---

## 1. B3 runtime evidence — Blocker A: CLOSED

`docs/builder/knowledge/B3-POSTGRES-16OF16-RUNTIME.txt` (also present at
repo root, byte-identical — confirmed via `diff`) was inspected.

- SHA-256: `271a88b436d9b7088466ab401f59abeb5dcf48443d5a42dc0c236cdd4efa27f7`
- File preserved unchanged — not edited, reconstructed, or reformatted.
- Content is consistent with genuine `node --test` output: per-test names
  match the B4.1.5 checklist 1:1 in order, timings are realistic and
  non-uniform (30ms for the placeholder-conversion unit test up to ~21s
  for the organization-registry integration test), the connection target
  is a real-looking Neon pooler hostname, and the SSL-mode deprecation
  warning text is exact, specific `pg`/`pg-connection-string` library
  wording — not something that reads as invented.
- Exact summary block:
  ```
  ℹ tests 16
  ℹ suites 0
  ℹ pass 16
  ℹ fail 0
  ℹ cancelled 0
  ℹ skipped 0
  ℹ todo 0
  ℹ duration_ms 145293.863778
  ```
  This matches the required 16/16 PASS with zero fail/skip/cancelled exactly.

I cannot independently re-execute this against the same Neon instance from
this sandbox (still no `COZY_DATABASE_URL`, still no network egress), so
this is a document inspection, not a live re-verification — but that is
exactly what closing an evidence gap with a supplied artifact means, and
the artifact itself is now genuinely present in the checkpoint rather than
described secondhand.

**BLOCKER A: CLOSED.**

## 2. Changed-file inventory — Blocker B: STILL OPEN

Confirmed by direct diff against the prior checkpoint's full file tree:
the **only** difference between this evidence bundle and the previously
reviewed checkpoint is the addition of the two identical runtime-evidence
copies. No new report, manifest, or documentation addressing the 6
previously-flagged files was included.

**The 6 files remain unexplained, unchanged from the last gate:**
- `index.html`
- `chalzydashboard.html`
- `core/shell/cozy-workspace.js`
- `login.html`
- `Firebase/firebase-auth.js`
- `core/modules/identity/auth-coordinator.js`

Per your own framing of this remediation request — the runtime evidence
"does NOT automatically resolve the unexplained changed-file inventory" —
this is exactly what happened: it resolved Blocker A only.

**BLOCKER B: still OPEN.**

## 3. pg dependency — PASS (now with an installed tree to check against)

This evidence bundle, unlike the original checkpoint, includes an actual
`node_modules/`. That let me verify the *installed* versions, not just the
lockfile's declared ones:

| Package | Lockfile | Installed |
|---|---|---|
| pg | 8.23.0 | 8.23.0 ✓ |
| pg-connection-string | (transitive) | 2.14.0 ✓ present |
| pg-pool | (transitive) | 3.14.0 ✓ present |
| pg-protocol | (transitive) | 1.16.0 ✓ present |
| pg-types | (transitive) | 2.2.0 ✓ present |
| pgpass | (transitive) | 1.0.5 ✓ present |
| playwright | 1.62.1 | 1.62.1 ✓ |

Installed tree matches the lockfile exactly. No conflicting Postgres
client packages found.

## 4. node_modules / secret hygiene

- **New observation, non-blocking but worth flagging clearly:** this
  evidence bundle (unlike the original `-CERTIFIED-16of16.zip` checkpoint)
  **does package a full `node_modules/` directory** (~20 MB). The original
  B3-certified checkpoint itself remains clean — this is a separate
  evidence bundle, not a resubmission of the baseline — but if this bundle
  is ever treated as the new baseline going forward, `node_modules/`
  should be excluded before it's certified. Per B4.1.3, a locally-generated
  `node_modules` is fine for *testing*, not for what gets called a
  checkpoint.
- Scanned `node_modules` for `.env`/`.pem`/`.key` files and credential
  patterns: two matches, both inside `pg-pool`'s and
  `pg-connection-string`'s own README.md files, using literal placeholder
  values (`someuser`/`somepassword`, `DBuser`/`secret`) as documentation
  examples — not real credentials.
- No secrets found. No `.git/` directory present.

## 5. As-delivered SHA-256 manifest

Generated fresh from the full extracted tree (1,592 files, `node_modules`
excluded as a build artifact, not source):

`docs/builder/knowledge/AS-DELIVERED-SHA256-MANIFEST.txt`

This is the manifest this checkpoint was previously missing — going
forward this file, not `CP16B`, is the correct comparison baseline for the
*next* gate.

## 6. Production safety boundary — confirmed intact

No production database switch, schema change, data migration,
`DATABASE_URL` change, deployment, SQLite-fallback removal, or
authentication weakening occurred. All actions this turn were read-only
inspection plus generation of the manifest/report files listed above.

## 7. Final status

**STATUS = B4.1 BLOCKED**

One of two required gates now passes:

- [x] B3 16/16 runtime evidence located, inspected, and reconciled
- [ ] Changed-file inventory fully reconciled — **6 files still unexplained**

B4.1 cannot be marked VERIFIED and B4.2 does not begin until the 6
Group-B files above are accounted for with actual checkpoint evidence
(a changed-file report, implementation note, or equivalent — not another
timestamp inference from this side).
