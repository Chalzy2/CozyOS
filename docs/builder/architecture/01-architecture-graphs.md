# CozyOS — Architecture Graphs (Living Artifact)
Generated by Cozy Builder Observation Engine · Source: direct inspection of `dashboard.html`/`login.html`/`index.html` script order + coordinator source files. Every edge below was confirmed by reading a `#identity()`/`#session()`-style accessor, a `<script src>` tag, or an explicit "calls X" comment — none are guessed from naming alone.

## 1. Authentication & Session Dependency Graph
```mermaid
graph TD
    login[login.html / index.html / dashboard.html]
    ies[identity-engine.js]
    css[cozy-session-service.js]
    ac[AuthCoordinator<br/>core/modules/identity/auth-coordinator.js]
    sess[CozyOS.Session]
    idn[IdentityEngine]
    arp[AdminRecoveryPolicy]
    authn[CozyOS.Auth]
    authz[AuthorizationCoordinator<br/>core/security/auth-coordinator.js]
    ape[AuthPolicyEngine]
    afr[AuthFactorRegistry]

    login --> ies --> idn
    login --> css --> sess
    login --> ac
    ac -->|login/logout/restore| idn
    ac -->|establishFromIdentity / establishFromExternalAuth / end| sess
    ac -->|attemptNormalLogin / listAdminSessions / forceSignOutAllSessions| arp
    idn -.emits identity:session-created / identity:session-ended.-> authn
    arp -.emits.-> authn
    ac -->|getCurrentAdministrator| authn
    authz -->|authenticate / authorize| authn
    authz --> ape
    authz --> afr
```
**M372 fix, shown structurally:** before M372, `login.html`/`index.html` had `ac` wired but not `css`→`sess` — so `ac`'s calls into `sess` during `restoreSession()` hit a null dependency. The missing edge is now present on all three pages (verified).

## 2. Startup / Module-Loading Graph (dashboard.html, first 20 of 80+ script tags, in load order)
```mermaid
graph TD
    A[launch-sequence.js] --> B[cozy-theme.js]
    B --> C[cozy-background.js]
    C --> D[cozy-environment.js]
    D --> E[live-animation-engine.js]
    E --> F[startup-orchestrator.js]
    F --> G[living-particles/sounds/audio/ai]
    G --> H[cozy-connect.js / offline.js]
    H --> I[cozy-registry.js]
    I --> J[Firebase x8]
    J --> K[identity-engine.js]
    K --> L[admin-recovery-policy.js]
    L --> M[cozy-session-service.js]
    M --> N[cozy-auth.js]
    N --> O[firebase-session-bridge.js]
    O --> P[AuthCoordinator]
    P --> Q[security factor stack x12]
    Q --> R[AuthorizationCoordinator]
    R --> S[cozy-login-gate.js]
```

## 3. Plugin Graph
```mermaid
graph TD
    PM[PluginManager v1.2.0]
    PM --> Reg[register/unregister]
    PM --> Exec[isolated per-plugin execution<br/>crash threshold + auto-disable]
    Reg --> ShopOS[ShopOS: core/sales/inventory/purchasing/<br/>payments/bookkeeping/reporting/dashboard/reconciliation]
    Reg --> MpesaOS[MpesaOS: core/float/till/paybill/reporting]
    Reg --> WholesaleOS[WholesaleOS: core/customer/debt]
    Reg --> Single[ChurchOS-core / HospitalOS / PharmacyOS]
    PM -. lifecycle events .-> Events[cozyos:plugin:install/enable/disable/error/timeout/remove]
```

## 4. Module Graph (layer level, matches CORE_ARCHITECTURE.md's own stated layering)
```mermaid
graph LR
    Kernel[Kernel: Compatibility/Bootstrap/Lifecycle/Diagnostics<br/>⚠ orphaned — no HTML include found] -.would feed.-> Engines
    Engines[Shared Engines: identity, security, session,<br/>storage, connectivity, calculation, vault, media...] --> Apps[Applications: ShopOS, MpesaOS, QuarryOS,<br/>Developer Hub, ChurchOS*]
    Apps -. "*only 3 honestly registered<br/>per module-registry.js" .-> Note[HospitalOS/SchoolOS/ChurchOS/QuarryOS<br/>have code, not shell-integration]
```

## 5. Event Graph
See `04-event-catalog.md` for the full extracted list. Structurally, events flow: **engine emits → `platform-event-bus.js` (or native `CustomEvent`) → listeners in coordinator/UI layers**, never engine-to-engine direct calls for cross-cutting concerns (this pattern held in every coordinator file read).
