# Builder Handoff — M387.5 (Browser Verification & Integration)

**Status:** Implementation Verified: NO | Verification Verified: NO — 8 of 9 original findings 🟢 Fixed, 1 (MD-004) ⚪ Deferred, 1 new finding (AA-004) 🟡 Composed during this pass's own re-verification. See Repair Queue Summary and Findings tables below (Rule 61/62).
**Milestone Status:** **Partial Verification** (Rule 63) — Repair Queue contains a High-priority Composed item (`AA-004`) created by this milestone, so Completed status is not available regardless of how much else passed. See gate checklist below.

## Milestone Completion Gate (Rule 63)
- [x] All planned implementations finished
- [x] All syntax verification passes
- [x] Browser/device verification passes — partially (page-load clean, 5 rounds; interactive/mobile not started)
- [x] Regression verification passes
- [x] Integration verification passes
- [ ] Repair Queue has no High-priority Composed item created by this milestone (`AA-004` blocks this)
- [x] `RELEASES.md` updated
- [x] `LATEST.md` updated
- [x] `HANDOFF.md` updated
- [x] Repository and package hashes generated

## Certification
The Continuous Development Contract only defines YES/NO for certification
fields (no PARTIAL state exists yet). Recorded here as NO with explanation,
not rounded up.

- Repository Verified: YES
- Compose Verified: YES
- Implementation Verified: NO — Findings 1–7 and 9 (8 total) are 🟢 Fixed with full Compose→Plan→Implement→Verify→Close chains shown in `docs/history/M387.5.md`. `AA-004` is 🟡 Composed only — real, evidenced, but no safe single fix identified yet (Rule 52 implementation lock).
- Verification Verified: NO — page-load/console/network verification done in a real browser (Chromium via Playwright) across 5 rounds for `index.html`/`login.html`/`dashboard.html`; interactive auth-flow and mobile/Android verification have not entered even the Compose stage.
- Handoff Verified: YES
- Artifact SHA-256 Verified: YES
- Ready for Next Account: YES — next required phase is **M387.5b**, not M388.

## Repair Queue Summary
(Per Rule 62 — full log in `docs/builder/knowledge/repair-queue.md`.)

```
High:
- AA-004

Medium:
- MD-004

Low:
- None
```

## What This Milestone Did
Real-browser verification (real Chromium, not Node/vm) of the M372–M387
Living Engine chain, across 5 verification rounds. Found 9 real issues by
direct source inspection. Took 8 of them through the full Rule 61
Compose→Plan→Implement→Verify→Close lifecycle to 🟢 Fixed. Formalized the
9th (Finding 8) as `MD-004`, ⚪ Deferred — a deliberate non-repair, not a
failure, since building 3 new media engines is feature-scale work. While
re-verifying the 8 fixes, found and Composed 1 new finding (`AA-004`, an
`AudioEngine` naming ambiguity) that had been present since Round 2 but not
yet properly investigated — logged per Rule 52, no code written against a
guess. Adopted **Rule 61** (Finding Lifecycle) and **Rule 62** (Repair
Queue) this milestone specifically because fixing "all 15" instances of
Finding 6 turned out not to mean all instances — re-verification found 8
more.

## Findings — Composed (Not Yet Fixed)

| Finding ID | Description | Evidence | Priority |
|---|---|---|---|
| MD-004 | 3 media engine files (`background-engine.js`, `codec-encoding-engine.js`, `codec-decoding-engine.js`) imported by `media-pipeline-manager.js` but absent from the entire repository | Repository-wide `find` confirms absence; `net::ERR_ABORTED` ×3 in every browser round; full Rule 51 trail in `knowledge/missing-dependency-registry.md` | Medium — `EngineBridge` already fails this closed |
| AA-004 | `window.CozyOS.AudioEngine` claimed by two files (`cozy-audio-engine.js`, ES-module bridge's `audio-manager.js`) with conflicting ownership comments; 5 real call sites depend on whichever wins | `[EngineBridge] "audio" unavailable: ... already occupied` warning, present since Round 2; full 7-step Rule 52 record in `knowledge/architecture-ambiguity-registry.md` | **High** — top priority for M387.5b |

## Findings — Fixed

| Finding ID | Files changed | Tests passed | Verification evidence |
|---|---|---|---|
| 1 | `core/modules/developer/developer-hub.css` | n/a (CSS) | Browser: 0 `core/core/` requests, 0 theme-rejection warnings, Rounds 2–5 |
| 2 | `core/modules/speech/cozy-speech.js`, `core/modules/vision/cozy-vision.js` | `node --check` PASS (both) | Browser: 0 "already been declared" errors, Rounds 2–5 |
| 3 | `core/pluginManager.js` (`SEMVER_RE`, `_compareVersions()`) | `node --check` PASS | Browser: 0 "Invalid manifest.version" errors, Rounds 2–5 |
| 4 | `dashboard.html` (6 script tags added) | n/a (HTML) | Browser: 0 "Required internal modules are not loaded" errors, Rounds 2–5 |
| 5 | `dashboard.html` (1 attribute), `core/permissions.js` (2 lines removed) | `node --check --input-type=module` PASS (both) | Browser: 0 import/module-not-defined errors, Rounds 3–5 |
| 6 | `core/pluginManager.js` (+1 shared helper), 23 plugin/module call sites | `node --check` PASS on all 23 | Browser: 16 → 1 → 0 "executionHandler must be a function" errors across 2 sub-passes (15 files, then 8 more found on re-verification) |
| 7 | `index.html` (1 `<link>` added) | n/a (HTML) | Browser: 0 theme-rejection warnings on `index.html`, Rounds 4–5 |
| 9 | `core/bridge/engine-bridge-bootstrap.js` (1 registration removed) | `node --check` PASS | Browser: 0 `"fs"` resolution errors, Rounds 4–5 |

State legend (Rule 61): 🟡 Composed · 🟠 Planned · 🔵 Implementing · 🟢 Fixed · 🔴 Failed Verification · ⚪ Deferred.

## Files Modified
- `core/modules/developer/developer-hub.css` (Finding 1)
- `core/modules/speech/cozy-speech.js` (Finding 2)
- `core/modules/vision/cozy-vision.js` (Finding 2)
- `core/pluginManager.js` (Findings 3 and 6)
- `dashboard.html` (Findings 4, 5, and 6)
- `index.html` (Finding 7)
- `core/permissions.js` (Finding 5 cascade)
- `core/bridge/engine-bridge-bootstrap.js` (Finding 9)
- 22 additional plugin/module files for Finding 6 (see `docs/history/M387.5.md`'s Finding 6 section for the full list)

## Files Added
- `docs/history/M387.5.md` — full cause/evidence/fix/regression-verification detail, all rounds
- `Browser-Verification-Report.md`, `Integration-Report.md`, `Regression-Report.md` — standalone reports
- `RELEASES.md` — package-hash ledger (Rule 60)
- `docs/builder/rules/05-release-manifest-rule.md` (Rule 60), `06-finding-lifecycle-rule.md` (Rule 61), `07-repair-queue-rule.md` (Rule 62)
- `docs/builder/rules/00-INDEX.md` — master rules index
- `docs/builder/knowledge/repair-queue.md` — live Repair Queue (Rule 62)
- This file

## Honest Scope Corrections
- Did not touch `AA-004` — real, but no evidence yet says which of 2
  conflicting ownership claims is correct; a same-session rename would be
  guessing, not a repair (Rule 52 implementation lock).
- Did not build the 3 files behind `MD-004` — feature-scale work, correctly
  ⚪ Deferred rather than rushed or faked.
- Did not fabricate interactive auth-flow or mobile/Android verification —
  neither has entered the Compose stage; stated explicitly, not implied to
  be covered.

## Verification
- Round 1 (pre-fix): 5 confirmed defects on `dashboard.html`.
- Round 2/3 (post 5 fixes): each of the 5 fixes' specific error confirmed
  gone; 2 new errors appeared (masked-until-now, not regressions).
- Round 4 (post Findings 6/7/9 first pass): errors dropped from ~20 to 2
  (1 environment-limited Firebase + 1 remaining `executionHandler` error).
- Round 5 (post 8 more Finding-6 fixes): `dashboard.html` down to 1 error
  (Firebase, environment-limited) + 4 failed requests (Firebase CDN +
  3 `MD-004` files) — the target clean state, modulo documented exceptions.
- Engine startup order (`LivingSecurityCoordinator` → ... →
  `LivingDecisionEngine`): unchanged and confirmed present, in order, no
  duplicates, across all 5 rounds.
- IndexedDB: `cozyos-identity` v8 confirmed present across all 5 rounds
  (existence only — functional per-store read/write not yet verified).

## Evidence
RP: 12 (RP-007 through RP-012 added this pass) | RG: 0 | SF: 4 | MD: 4 (`MD-004` added this pass) | PF: 1 | AA: 4 (`AA-004` added this pass) | DI: 3 | DC: 3

## SHA-256
See `RELEASES.md` for the current, authoritative repository-wide hash
(computed over every file except `RELEASES.md` itself) and the package
hash for this delivery — per-file hashes are not repeated here to avoid a
stale-list drift risk across this milestone's several rounds of edits.

## Resume Instructions For M387.5b
1. Read `docs/builder/rules/00-INDEX.md`, then `docs/builder/knowledge/repair-queue.md`.
2. Per Rule 62, pick the highest-priority open item: `AA-004`.
3. Plan → Implement → Verify → Close `AA-004` — read `cozy-hearing.js`'s and
   `live-capture-engine.js`'s real method calls against both candidate
   engines' real APIs before choosing a fix; do not rename either file on
   assumption.
4. Decide `MD-004`'s disposition — stays ⚪ Deferred, or gets its own
   dedicated compose pass.
5. Run interactive auth-flow verification and mobile/Android verification
   from scratch — neither has entered the Compose stage yet.
6. Update the Repair Queue, `RELEASES.md`, `LATEST.md`, and `HANDOFF.md` the
   moment any item's status changes.
7. Only after `AA-004` is 🟢 Fixed and interactive/mobile verification
   passes should certification move to full `Implementation Verified: YES`
   / `Verification Verified: YES`, and only then should M388 begin.
