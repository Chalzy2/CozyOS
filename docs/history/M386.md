# Builder Handoff — M386 (Living Device Intelligence Engine)

**Status:** Implementation Verified: NO (Node-verified only, browser/device runtime verification still outstanding since M375)

## Certification
- Repository Verified: YES — extracted CozyOS-M385-repo-handoff.zip, confirmed all 9 composed systems (TrustedDeviceManager, IdentityEngine, LivingTrustEngine, LivingRiskEngine, LivingBehaviorEngine, LivingSecurityCoordinator, LivingAIContextEngine, PlatformEventBus) exist for real before writing any code.
- Compose Verified: YES — no new fingerprinting/hashing/trust/risk logic; reuses TrustedDeviceManager.generateFingerprint() and IdentityStorage as-is.
- Implementation Verified: NO — real Node vm 3-scenario functional pass (new→known transition, false-positive-free change detection on volatile fields, true-positive change detection on a non-identity signal), browser never run.
- Verification Verified: NO
- Handoff Verified: YES
- Artifact SHA-256 Verified: YES (below)
- Ready for Next Account: YES

## Real ownership finding, resolved before implementation
"Cozy Environment" in the compose list refers to `core/living/cozy-environment.js`, which is real but is a weather/lighting facade over the Living Background — it has no browser/device signals. Not composed for device profiling (would be a fabricated relevance). Flagged here rather than silently forced into the composition.

## Files Added
- `core/security/living-device-intelligence-engine.js`

## Files Modified
- `core/modules/identity/identity-storage.js` — additive `deviceProfiles` store, DB_VERSION 6→7 (same precedent as M383/M384; upgrades in place, no other store touched)
- `core/security/living-risk-engine.js` — one-line additive: `cozy:device-risk` added to its existing real-event recalculation list (no other logic touched)
- `dashboard.html` — 1 script tag

## Do-Not-Implement compliance
Root/jailbreak detection, SIM info, Bluetooth/USB devices, hardware serial, debugger detection, memory-tamper detection — none implemented; none are real standard browser APIs. Listed explicitly in every returned `deviceProfile.unavailableSignals` rather than omitted silently.

## Honest scope disclosure
`failedLogins` stays in the persisted schema for forward compatibility but is never incremented — confirmed by search that no "identity:login-failed" (or equivalent) event exists anywhere in identity-engine.js today. Documented, not fabricated, matching M384's disclosure style for absent signals.

## Verification
- Syntax: PASS — 493/493 JS files (repo-wide sweep, excluding `_archive/`), 0 errors.
- Duplicate-engine: PASS — 1 `window.CozyOS.LivingDeviceIntelligenceEngine` assignment, 1 `Modules["living-device-intelligence-engine"]` registration, 1 dashboard.html script tag.
- Dependency: PASS — real load order (IdentityStorage → TrustedDeviceManager → LivingRiskEngine → LDIE) loads with zero errors under Node `vm`.
- Event: PASS (functional, in-memory-stubbed storage) — new device → `cozy:device-new`; same device, no change → `cozy:device-known`, no `cozy:device-updated` noise; same device, real signal change (language) → `cozy:device-known` + `cozy:device-updated` with an accurate field diff; changing a signal that TrustedDeviceManager's own hash also uses (userAgent) correctly produces a *new* fingerprint/new device, not a false "update" — confirmed intentional, not a bug.
- Storage: PARTIAL — IdentityStorage's real persistence is IndexedDB-backed (browser-only); confirmed via Node `vm` that it fails closed (caught, non-fatal) rather than throwing, but true persistence across calls could only be verified in Node using an in-memory storage stub. Real IndexedDB persistence is Implementation Verified: NO until real browser verification, same as every Living* engine since M375.

## Evidence
RP: 6 | RG: 0 | SF: 4 | MD: 3 | PF: 1 | AA: 3 | DI: 3 | DC: 3

## SHA-256
- living-device-intelligence-engine.js: 10b450425613d4b3c71ef98cda0b1734185454e6fd85f1fffed9a0a22c516925
- identity-storage.js: 5dd2fdce4959d9ad6be6252d6dab302c537c92052ff3216821af1f1f5221b273
- living-risk-engine.js: 0d71b17a70847157d7d2e3da35d82bd84d09b59b787ddb7afcf55a67673865ff
- dashboard.html: 4ff0f3cf6ec004289e199e3fa631a2460590a68b02029b35f6426301f876142c
- Package (CozyOS-main-v2_25_14-M386-CERTIFIED.zip): see final chat message (hashing the ZIP after this file is written changes the ZIP's own hash — same chicken-and-egg noted in M385's HANDOFF.md)
