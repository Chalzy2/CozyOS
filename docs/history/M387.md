# Builder Handoff — M387 (Living Decision Engine)

**Status:** Implementation Verified: NO (Node-verified only, browser/device runtime verification still outstanding since M375)

## Certification
- Repository Verified: YES — read every composed engine's real, current public API (LSE, LRE, LTE, LBE, LDIE, LAICE, AuthPolicyEngine, PlatformEventBus) directly from this session's M386 baseline before writing any mapping logic.
- Compose Verified: YES — recalculates no other engine's trust/risk/behavior/device number; reuses `LivingSecurityCoordinator.decideAuthentication()`'s tier and `LivingRiskEngine.evaluate()`'s level verbatim as the primary decision-level drivers.
- Implementation Verified: NO — real Node `vm` load of the full 8-file real dependency chain + 2 functional decision runs against real (not mocked) engine logic, browser never run.
- Verification Verified: NO
- Handoff Verified: YES
- Artifact SHA-256 Verified: YES (below)
- Ready for Next Account: YES

## Real ownership finding, resolved before implementation
`LivingSecurityCoordinator` already computes a 6-tier authentication decision, and `LivingAIContextEngine` already computes suggestion strings — neither recalculated. LDE re-maps both real outputs, together with Trust/Behavior/Device numbers no single engine below combines, into the spec's 7-level vocabulary. The full mapping table lives in `#decideLevel()`, disclosed in the file header rather than hidden.

`AuthPolicyEngine` has no generic "current auth state" getter — only `evaluate(operationName, context)` against a pre-defined policy. LDE only composes it when a real `operationName` is supplied by the caller; otherwise honestly reports unavailable rather than guessing an operation name.

## Files Added
- `core/security/living-decision-engine.js`

## Files Modified
- `core/modules/identity/identity-storage.js` — additive `decisionHistory` store, DB_VERSION 7→8 (same precedent as M383/M384/M386)
- `dashboard.html` — 1 script tag

## Honest scope disclosure
`recordOutcome()` and the persisted decision+outcome history are real and functional (confirmed via Node), but no real caller anywhere in this repository invokes it yet — no UI/flow reports back whether a recommendation succeeded. Documented as a genuine gap, matching this codebase's established disclosure pattern, rather than fabricated.

A brand-new user with zero enrolled factors and a freshly-seeded trust score lands on **Restrict** on first decision (trust score < 20 branch) — this is real behavior traced to the real trust score LivingTrustEngine returns, not a defect, but worth Charles's review as a policy-tuning question (onboarding UX vs. security posture) before this drives any real UI.

## Verification
- Syntax: PASS — 494/494 JS files (repo-wide sweep, excluding `_archive/`), 0 errors.
- Duplicate-engine: PASS — 1 `window.CozyOS.LivingDecisionEngine` assignment, 1 `Modules["living-decision-engine"]` registration, 1 dashboard.html script tag.
- Dependency: PASS — real 8-file load order (TrustedDeviceManager → LDIE → LRE → LTE → LBE → LSE → LAICE → LDE) loads with zero errors under Node `vm`.
- Event: PASS — `cozy:decision-ready` fires on every `decide()` call; `cozy:decision-warning` correctly fired for a real "Restrict" result; `recordOutcome()` confirmed functional against real in-memory-stubbed storage.
- Explainability: PASS — every returned decision includes `reasons` (each citing the real composed value that drove it), `evidence` (the real raw output of every composed engine), and `contributingEngines` (real availability flags) — confirmed by inspecting actual returned objects across two real scenarios, not asserted from code review alone.
- Storage: PARTIAL — same real IndexedDB-is-browser-only limitation as M386; Node-verified via in-memory storage stub only.

## Evidence
RP: 6 | RG: 0 | SF: 4 | MD: 3 | PF: 1 | AA: 3 | DI: 3 | DC: 3

## SHA-256
- living-decision-engine.js: 7ee4eabbf5290484ade4dd335e2a52b694048a20624d923a01aa4027d7409772
- identity-storage.js: 1fad3217ef114ebd2e089e4bab57b055466c14a946fde4e583804749365055e1
- dashboard.html: 2830358ab9dba7bfba0b4b9dde5dd3f893cb7c95b30c7e228d3ad7b87aad0cdc
- Package: see final chat message (hashing the ZIP after this file is written changes the ZIP's own hash)
